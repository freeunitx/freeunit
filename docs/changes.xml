<?xml version="1.0" ?>
<!DOCTYPE change_log SYSTEM "changes.dtd" >


<change_log title="unit">


<changes apply="unit"
         ver="1.36.2" rev="1"
         date="2026-08-30" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change type="bugfix">
<para>
an application worker that served a WebSocket upgrade never became idle again.
The router counts a session on the worker and keeps a worker with a session out
of the idle queues, but nothing uncounted the session when it ended, so the
process stayed out of them for good: its idle timer never ran and its slot
counted against "processes": {"max"} until it died, which an application that
serves WebSockets reaches one upgrade at a time.  A session is now uncounted
when it ends, whichever side ends it; the bug had appeared in 1.19.0.
</para>
</change>

<change type="bugfix">
<para>
an application that answered a request and kept running was counted idle.  PHP
does this whenever a script calls fastcgi_finish_request(): the response goes
out, the script runs on, and the router returned the worker's slot to
"processes": {"max"} and let its idle timer reap a process that was still
executing.  Such a worker now reports itself busy until the work ends, so "max"
bounds live processes and "/status" reports them as "detached".  The same holds
for a request whose "limits": {"timeout"} deadline has passed: the worker still
running it is no longer returned to the idle pool either, and keeps counting
against "max" until it answers or exits;
the bug had appeared in 1.29.0.
</para>
</change>

<change type="change">
<para>
"limits": {"timeout"} now also bounds how long a request waits for an
application process, not only how long a worker may spend on it.  A request
that waited for capacity previously had no deadline at all.  The default
remains no timeout.
</para>
</change>

<change type="security">
<para>
the string form of "access_log" "format" wrote what a variable expanded to
without escaping it.  "$uri" is the percent-decoded target, so a request for
"/%0d%0a..." put a real CRLF in the log and the rest of the line read as a
record of a request that never happened.  Bytes a variable expands to are now
escaped the way nginx escapes them: the quote, the backslash, every byte below
0x20, 0x7F and every byte above it all become "\xHH".  nginx emits no short
forms, so a reader that already unescapes an nginx log reads this one.  The
text of the
format itself is unchanged, except for an "njs" format, which produces the
whole record and is escaped whole.  A value that carries a quote of its own now
shows it escaped: an entity-tag logged through "$response_header_etag" reads
\x22abc\x22 rather than "abc", and a non-ASCII path reads "\xD0\xBF..." rather than
its bytes.  A log parser that expected the raw bytes has to be updated, though "\xHH" is
reversible and the bytes can be recovered from it.  The object form of "format"
is unaffected by this change, but it is not a way to keep bytes either: it
replaces anything that is not valid UTF-8 with U+FFFD before it serializes.
</para>
</change>

<change type="security">
<para>
a port message that was queued because the peer's socket was not writable
borrowed the descriptors it carried -- for NEW_PORT, another port's socket
and queue -- so a port closed before the queue drained left the deferred
sendmsg() naming a closed number, or an unrelated descriptor that had since
reused it.  The queued copy now owns duplicates and closes them after the
send (#388).
</para>
</change>

<change type="bugfix">
<para>
a process could stop hearing from a peer for good.  When the wake-up that
tells a peer to read the shared queue failed because the kernel could not
allocate for it, the port treated the peer as dead: the wake-up was dropped
and, since a later message raises one only when the queue was empty, no
later message on that port raised one either.  The wake-up is now kept and
retried, and the retry no longer waits for a socket event that a socket that
never filled up will not deliver (#392).
</para>
</change>

<change type="change">
<para>
upgrade contrib njs to 1.0.1.
</para>
</change>

<change type="change">
<para>
removed the unused job cluster (nxt_job.c, nxt_job.h, and
nxt_event_conn_job_sendfile.c).  The offload path they implemented had no
caller anywhere in the tree and its thread pool handoff was structurally
unreachable, since the connection field it read was never assigned.
</para>
</change>

<change type="feature">
<para>
byte-range requests for static files.  A "share" action answers a "Range"
header naming a single range with "206 Partial Content" and a
"Content-Range" header, an unsatisfiable one with "416 Range Not
Satisfiable", and advertises support with "Accept-Ranges: bytes".
"If-Range" is honoured, so a range is served only while the client's copy
is still current.  A request naming several ranges is answered in full, as
a server that does not implement multipart ranges may.
</para>
</change>

<change type="feature">
<para>
conditional requests for static files.  A "share" action now answers a GET
or HEAD carrying "If-None-Match" or "If-Modified-Since" with "304 Not
Modified" when the file's validators still match, so a client that already
holds the file is not sent it again.  Unit emitted "ETag" and
"Last-Modified" before, but ignored both when they came back.  "If-Match"
and "If-Unmodified-Since" are evaluated first and answer "412 Precondition
Failed" when they do not hold.  Entity-tag comparison is weak for
"If-None-Match" and strong for "If-Match", and "*" matches any
representation, per RFC 9110.
</para>
</change>

<change type="bugfix">
<para>
the router died on the first request after a configuration that had a
"compression" block was replaced by one that had none.  The compression
state was held in process globals allocated from the router configuration
that parsed them, and a later configuration neither refilled nor cleared
them, so the next request read a freed pool.  The state is now held per
configuration and reached through the request, and the compressor a
response is already using is kept with that response, so a body still
being compressed when the configuration is replaced also finishes (#167).
</para>
</change>

<change type="bugfix">
<para>
a static response that was subject to content negotiation now carries
"Vary: Accept-Encoding".  Without it a shared cache could store whichever
representation it saw first and serve it to every client -- gzip bytes to one
that cannot decode them, or the uncompressed copy to one that could have had
the small version.  The header is sent on the identity response as well as
the compressed one, since the identity response is the one a cache must not
reuse for a client that accepts gzip.
</para>
</change>

<change type="change">
<para>
a content-coded response now carries a weak entity-tag, so "If-Range" cannot
resume a coded representation; "If-None-Match" revalidation is unaffected.
Unit derives the tag from the file's mtime and size, which do not change with
the content coding, so a gzip response and the identity one previously
advertised the same strong validator -- 1.36.1 and earlier shipped that.
RFC 9110 requires a strong validator to change whenever the selected
representation changes, and nginx weakens the tag for the same reason.
</para>
</change>

<change type="security">
<para>
a file descriptor is no longer leaked when a static request cannot be
satisfied by any acceptable representation.  A share with compression
configured answered "406 Not Acceptable" without closing the file it had
opened, so a single unauthenticated request carrying
"Accept-Encoding: identity;q=0, *;q=0" cost the router a descriptor, and
repeating it exhausted the table, stopping every application behind that
router.  Present since compression was added in 1.35.0: 1.35.0, 1.35.1,
1.35.2, 1.35.3, 1.35.4, 1.35.5, 1.36.0 and 1.36.1 are all affected.
</para>
</change>

<change type="security">
<para>
a file descriptor sent to a port is no longer leaked when the message is
dispatched and nothing takes ownership of it.  An application holds the write
end of the router's main port and the message header is unauthenticated, so a
compromised application could attach a descriptor to messages the router does
not expect one on -- or to a fragment that never reaches a handler -- and
consume the router's descriptor table, stopping every application behind it.
A fragment stream that is started and never completed still retains its
descriptor; that needs a bound on pending fragments and is tracked in
freeunitorg/freeunit#343.
</para>
</change>

<change type="change">
<para>
two source files that the build never used have been removed.
src/nxt_job_cache_file.c is named nowhere in auto/, so no configuration ever
compiled it, and nothing in the tree referenced it or its symbols.
src/nxt_source.h was removed because nothing includes it: of the 106 headers
in src/, it was the only one with no #include anywhere in the tree.  Nothing
observable changes.
</para>
</change>

<change type="change">
<para>
the fiber implementation has been removed.  It had not been compiled since
2017: the only allocation of the engine's fiber stack sat under "#if 0", so
the pointer was always NULL and the setjmp() branch that tested it could not
run.  The thread, log and sprintf sites were already disabled.  Nothing
observable changes.
</para>
</change>

<change type="change">
<para>
the nxt_dyld wrapper around dlopen()/dlsym()/dlclose() has been removed.
It shipped in the initial import and was never called: the language
module loader in src/nxt_application.c calls dlopen() and dlsym() itself
and never used this wrapper.  libnxt.a loses one object file; there are
no other callers.
</para>
</change>

<change type="bugfix">
<para>
do not exit when the capget() syscall is denied.  A syscall filter that
omits capget -- a systemd "SystemCallFilter=" allowlist, a hand-written
seccomp profile -- made a non-root unitd fail to start with "failed to get
process capabilities".  Unit now warns and runs without assuming any
capability: user and group switching and "rootfs" isolation are disabled
for applications that do not enable the "credential" namespace, and
applications run as unitd's own uid.  Every other capget() failure
remains fatal.
</para>
</change>

<change type="bugfix">
<para>
the main process did not answer a START_PROCESS request it could not carry
out -- an unknown router port, a sender that is not the router, or an
allocation that failed -- so the start stayed pending.  The application then
parked every later start on a prototype that was not coming, and the
requests waiting on it were never failed.
</para>
</change>

<change type="bugfix">
<para>
a message that could not be sent because the receiving process had died was
reported to the sender as sent.  A sender that answers a request and then sends
a fallback lost both, so an application could wait for an answer that nothing
would ever deliver.
</para>
</change>

<change type="bugfix">
<para>
eight places whose memory nothing else reclaims handed a message to a port and
did not take it back when the port refused it.  The port layer accepts a buffer
only when it answers success, so on a refusal -- which happens when memory runs
out, or when the receiving process has stopped reading a full shared queue --
the memory was lost.  Two of them lost shared memory that no cleanup reclaims,
so an application could lose the capacity to answer at all.
</para>
</change>

<change type="change">
<para>
a websocket connection is now closed when a frame cannot be handed to the
application.  Such a frame was previously dropped in silence, leaving the
peer's stream short a frame it was never told about -- which for a fragmented
message the client cannot detect.
</para>
</change>

<change type="bugfix">
<para>
a port could stop sending after it ran out of memory.  When a write had to be
retried later, the port asked its own thread to watch the socket again, and
that request needed memory of its own.  If it failed, nothing watched the
socket and every later message waited in the queue until the port closed.  The
port now carries the request it needs, so it cannot fail for lack of
memory.
</para>
</change>

<change type="bugfix">
<para>
the port layer reported a message as sent when it had been neither sent nor
queued.  This happened when a socket answered EAGAIN and no memory was left to
hold the message for a later attempt, so it takes memory pressure to reach.
The sender then cleared the state that would have
retried or reported it, so a request could wait for an answer that nothing
would ever deliver.
</para>
</change>

<change type="bugfix">
<para>
an application worker that died before it finished starting was reported to
nobody, so the start it was forked for stayed pending forever.  Once the
application reached its "processes" maximum it started no further process,
and because an application's "timeout" defaults to 0 the requests waiting on
it hung instead of failing.
</para>
</change>

<change type="security">
<para>
application processes no longer inherit unitd's capabilities.  A unitd
started as a non-root user that had been granted capabilities -- systemd
"AmbientCapabilities=", file capabilities -- passed them through fork()
to every application it ran, because switching between two nonzero uids
does not clear them and a filtered capget() skips the switch entirely.
Each forked process now empties its permitted, effective, inheritable
and ambient sets once its credentials are final, before the worker
serves any request.  The language module's own startup still runs before
the drop: the mount, pivot_root and chroot work for "rootfs" isolation
happens after it and needs the capabilities that are being given up.
Where a syscall filter denies capset() as well, an application start is
now refused rather than allowed to proceed with capabilities still held:
the request is answered 503 and the log names the denied syscall.  A
process with an empty permitted, effective and ambient set -- which is
every process of a root unitd once it has switched away from uid 0, and
every process of an unprivileged one that was granted no capabilities --
keeps behaving as before, since there is nothing an application could
have inherited from it.  Its inheritable set is emptied as well, which
on its own is not counted and grants nothing without an execve() of a
file carrying a matching capability.  The main process is
unchanged, since it needs CAP_NET_BIND_SERVICE to bind listening sockets
on every reconfiguration; the router, controller and discovery processes
warn and carry on rather than refuse, because they run no application
code.  Applications configured with "user": "root" are also unchanged,
since a root process regains capabilities on the next execve() anyway.
One configuration does lose something it previously kept: an application
in a "credential" namespace began with a full capability set inside that
namespace, because the default uid_map maps unitd's euid to the
application's uid, so the setuid() is not a transition out of uid 0 and
nothing cleared it.  Those capabilities were valid only inside the
namespace and Unit's own use of them is finished before the drop, but an
application that relied on them now needs "user": "root".
</para>
</change>

<change type="bugfix">
<para>
unitctl gave up on a configuration it could not decode as UTF-8.  Unit stores
the bytes it is given, so a configuration can hold bytes no UTF-8 decoder
accepts; "unitctl execute" and "unitctl export" then failed with "JSON error
[path=/config]" and left the operator no way to read or back up the
configuration.  Both work now: the document is shown with the undecodable
bytes replaced and the members they fall in named, and "export" stores the
server's bytes verbatim.  "unitctl edit" refuses such a configuration rather
than writing the replacements back over it.
</para>
</change>

<change type="bugfix">
<para>
unitctl reordered the members of a configuration.  "unitctl edit" and "unitctl
export" passed the document through an unordered map, so members came back in
an arbitrary order and an edit rewrote the whole configuration even where
nothing had changed.  The order the server sent is now kept.
</para>
</change>

<change type="security">
<para>
unitctl no longer depends on rustls-pemfile or on hyper's "http2" feature.
rustls-pemfile is unmaintained; the parser is replaced by rustls-pki-types,
already in the dependency graph.  PEM parsing behavior is unchanged: both
parsers were run over 1884 PEM documents and produced zero differences.  This
closes RUSTSEC-2025-0134.  Dropping the "http2" feature from hyper, hyper-rustls
and hyper-util removes the h2 and fnv crates as well, closing
RUSTSEC-2026-0258, an unbounded empty DATA frame flood in h2 0.4.15.  The
control API speaks HTTP/1.1 only -- unitd writes "HTTP/1.1" into every response
line -- so HTTP/2 support was never usable against the server itself.
</para>
</change>

<change type="change">
<para>
unitctl no longer offers HTTP/2 in the TLS handshake it makes to a remote
control API.  This follows from the "http2" feature removal above.  A TLS-
terminating proxy placed in front of a remote control API must now speak
HTTP/1.1 to unitctl; one that only accepts HTTP/2 can no longer be reached.
</para>
</change>

<change type="change">
<para>
unitctl no longer reads hjson or YAML, and no longer writes YAML.  Input piped
to "/config" on stdin is parsed as JSON now, not hjson; hjson is a superset of
JSON, so piping JSON still works, but comments and unquoted keys do not.  A
".hjson" or ".cjson" file is refused with "hjson is no longer supported:
convert the file to JSON first", instead of being handed to the JSON parser,
which reports a misleading error at the first comment.  A ".yaml" or ".yml"
file is refused with "YAML is no longer supported: convert the file to JSON
first, for example with "yq -o=json"".  Supported input formats are now json,
json5 and pem.  "unitctl status -t yaml" is gone; --output-format now accepts
json, json-pretty and text.  This removes the nu-json dependency and the 30
crates of Nushell utility code it pulled in for a tool that speaks one JSON
control API.  It also removes serde_yaml and unsafe-libyaml; the latter is a
c2rust transpile of libyaml with 218 unsafe functions and no RustSec advisory.
tools/unitc is unaffected: it still converts configuration to and from YAML,
using yq.
</para>
</change>

<change type="change">
<para>
unitctl builds TLS support behind a "tls" Cargo feature, on by default.  The
musl build turns it off, so the static binary no longer links a C TLS library
and no longer accepts an "https://" control socket address; it refuses one with
a message naming the feature.  A Unix socket or a plain "http://" address is
unaffected, and a build with default features behaves as before.
</para>
</change>

<change type="change">
<para>
removed src/nxt_queue.c, the unused half of a queue utility library carried
over from nginx.  It defined nxt_queue_sort() and nxt_queue_middle(), both
exported but never called anywhere in the tree; src/nxt_queue.h and the
nxt_queue_* macros it provides are unaffected and untouched.
</para>
</change>

<change type="change">
<para>
nxt_mem_zone, a second allocator, is removed.  It was a zone allocator over a
mapped region, distinct from nxt_mp, the pool allocator product code actually
uses; nothing in the router, applications, or any language module called it.
Its only caller was its own unit test, run by "./build/tests"; that test is
removed too, dropping the three "mem zone test passed" lines from the suite's
output.  Upstream nginx/unit had already reached the same conclusion and
deleted the file as part of an unrelated cleanup.
</para>
</change>

<change type="change">
<para>
the entity-tag and last-modified date of a static file are now weak while
the request falls inside the second the file was last written, because a
rewrite to the same size during that second changes neither validator.
If-Match and the If-Range forms stop matching in that window, so a range
conditioned on If-Range falls back to the full response; an unconditional
Range is unaffected.  The tag keeps its format, so nothing already cached is
invalidated.
</para>
</change>

<change type="change">
<para>
unitctl sends a JSON configuration file as it was written.  It parsed the file
and re-serialized it before, which kept the last of two object members with the
same name and re-spelled numbers, so the server never saw what the operator
wrote.  Unit now reports the duplicate itself, with the line and column of the
file.  A file that is not valid UTF-8 is no longer refused by unitctl; Unit
refuses it and names the member.  The "edit" command no longer accepts JSON5 comments in the temporary
file.
</para>
</change>

<change type="change">
<para>
removed the GnuTLS, CyaSSL, and PolarSSL backends (nxt_gnutls.c,
nxt_cyassl.c, nxt_polarssl.c) and their configure plumbing ("--gnutls",
"--cyassl", "--polarssl").  None of the three ever built: they read a
"c-&gt;u.ssltls" union member that does not exist, and reference types such
as "nxt_ssltls_conf_t" and "nxt_event_conn_io_t" that predate a refactor
still visible in the current tree, so passing the flag reaches a compiler
error instead of a clean "unsupported".  CyaSSL was renamed wolfSSL and
PolarSSL became mbedTLS, both in 2015, so the flags also target products
that no longer exist under these names.  The OpenSSL backend is
unaffected; none of these files were ever part of a default build.
</para>
</change>

<change type="change">
<para>
unitctl no longer reads JSON5.  A ".json5" file is refused by name, with a
message saying to convert it first; "json5 -o config.json config.json5" does
that, and "jq" reformats ordinary JSON.  Nothing parsed JSON5 after the
configuration upload path stopped parsing its input, so this removes a parser
that only the refusal path still reached, and two crates with it.
</para>
</change>

<change type="change">
<para>
removed the PHP TrueAsync code path.  It was written for a "zend_async_event_t"
API expected in PHP 8.5 core, which did not ship it, so the configure probe
never succeeded and the code sat behind "#if NXT_PHP_TRUEASYNC" in every build.
It could not have compiled had the probe succeeded: it reads "async" and
"entrypoint" members that "nxt_php_app_conf_t" does not have and calls
"nxt_php_extension_init()", which no file in the tree defines.  The probe, the
"entrypoint" target key and the TrueAsync test suite are removed with it; a PHP
application builds and behaves as before.
</para>
</change>

<change type="bugfix">
<para>
the Python ASGI module refused a WebSocket message larger than 1 MB whatever
"max_frame_size" was set to.  The module carried a private 1 MB limit that no
configuration ever wrote and charged the fragments of a message against it,
while the configured value is checked by the router per frame: a 4 MB message
was closed with 1009 where "max_frame_size" was 32 MB, and at the default size
a client that split a larger message into small frames was refused as well.
What a message may be is now decided by "max_frame_size" alone, bounded only by
a 10 MB limit on how much of an undelivered message one connection may hold.
A fragmented message also left a count behind that lowered the limit applied to
the next message.
</para>
</change>

</changes>


<changes apply="unit-php unit-php8.3 unit-php8.4 unit-php8.5
                unit-python unit-python2.7
                unit-python3.8 unit-python3.9 unit-python3.10 unit-python3.11
                unit-python3.12 unit-python3.13 unit-python3.14
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc11 unit-jsc17 unit-jsc18
                unit-jsc19 unit-jsc20 unit-jsc21
                unit-wasm"
         ver="1.36.2" rev="1"
         date="2026-08-30" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change>
<para>
FreeUnit updated to 1.36.2.
</para>
</change>

</changes>


<changes apply="unit"
         ver="1.36.1" rev="1"
         date="2026-08-28" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change type="feature">
<para>
configuration validation errors now say where they are.  A rejected
configuration request carries an RFC 6901 JSON Pointer in the "path" member
of "location": it points at the member whose value failed validation, or at
the containing object when the failure is an unknown parameter, whose name
the message itself already carries; "" is the document root.  On an unknown
parameter whose name is a close and unambiguous typo of a known one, the
intended name is reported in a top-level "suggestion" member.  The addition
is strictly additive: the existing response fields and the error message
wording are unchanged, and each new member is omitted where it does not
apply.
</para>
</change>

<change type="feature">
<para>
the /status API reports OpenTelemetry span export health as "telemetry":
{"spans": {"exported": N, "failed": M}} -- the spans the collector accepted,
and the spans whose export failed.  Both are reported because a failure count
alone cannot distinguish a healthy pipeline from one that has never exported
anything.  The object is omitted entirely unless Unit was built with
OpenTelemetry support, "/config/settings/telemetry" is set, and the exporter
was built successfully, so its absence is how a client tells telemetry is off
rather than idle.  The counters are cumulative since the exporter was last
built, so changing any telemetry setting restarts them, while re-applying an
identical configuration does not.
</para>
</change>

<change type="bugfix">
<para>
validate the shared memory segment identifier and geometry an application
process supplies in its NXT_PORT_MSG_MMAP message.  The router checked only
the pid fields, then used the peer-authored header id directly as an array
index and mapped the descriptor at whatever length fstat() reported; a
compromised or faulty application could drive an out-of-range write and an
oversized mapping.
</para>
</change>

<change type="bugfix">
<para>
bounds-check the router-supplied chunk identifier and size in libunit before
consuming them, so an out-of-range size can no longer make every
serialized-pointer check in the arrival-time validation block pass and be
read out of bounds in the application process.
</para>
</change>

<change type="bugfix">
<para>
do not replay a stale SCM_RIGHTS control message in libunit when the
embedder's port_recv callback returns without receiving anything.  The out
parameter carrying the control-data length was stored unconditionally, so a
descriptor from an earlier message could be adopted a second time.
</para>
</change>

<change type="bugfix">
<para>
check the shared memory buffer allocation when forwarding a websocket frame.
Exhausting the outgoing shared memory crashed the router with a NULL
dereference; every other caller already checked.
</para>
</change>

<change type="bugfix">
<para>
keep the chunked request body buffer alive across framing reads.  A buffer
drained without producing a data slice was handed to its completion handler
while the parser still referenced it, a use-after-free reachable from an
ordinary chunked request.
</para>
</change>

<change type="bugfix">
<para>
parse the HTTP/1.1 trailer section (RFC 9112 7.1.2) instead of erroring on
the first byte after the terminal chunk.  In the proxy response relay this
aborted an already-streaming response and truncated the body at a racy
point; chunked requests carrying a trailer were rejected the same way.
Trailer field names and values are validated as they are parsed.
</para>
</change>

<change type="bugfix">
<para>
reject an application response that disagrees with itself about its own body
length.  A duplicate Content-Length was neither detected nor parsed, so every
such header was forwarded verbatim and the body was framed by the advertised
length instead of chunked.
</para>
</change>

<change type="bugfix">
<para>
release the response buffers adopted from the port on the error paths of the
router's response handler, and drain them from the request output chain when
the failure happens after they were linked into it.  Both leaked the chain
the handler had taken ownership of.
</para>
</change>

<change type="bugfix">
<para>
keep the memory pool cursor aligned in nxt_mp_get() by rounding the requested
size up to the maximum alignment rather than flooring it.  The small
allocation path is a bump allocator with no per-allocation alignment step, so
any request whose size was not a multiple of that alignment left the page
cursor misaligned, and allocations from that page kept violating the
documented alignment guarantee until a later size happened to bring the
cursor back into step, which crashed with SIGBUS on strict alignment targets
such as 32-bit ARM.
</para>
</change>

<change type="bugfix">
<para>
serialize the process reference count on the runtime process mutex and take a
reference during cross-thread process lookups.  The count was mutated
non-atomically from every engine thread and a lookup returned an unreferenced
process, so a router worker could operate on an nxt_process_t -- including its
shared memory segments and its mutex -- that the main thread had already torn
down.
</para>
</change>

<change type="bugfix">
<para>
keep the socket and timer tasks of a connection scoped to that connection, so
a work item cannot run against a task belonging to a different connection.
</para>
</change>

<change type="change">
<para>
remove the dead chunk-tracking bitmap from the shared memory segment header.
Its trailing fields ran past the header area and aliased the payload of the
segment's first chunk, which was harmless only because nothing read or wrote
them; the field offsets a peer of either vintage uses are unchanged, so old
and new builds still interoperate.  A static assertion now pins the header
struct inside the area reserved for it.
</para>
</change>

<change type="change">
<para>
the shared memory chunk range check is now a single shared implementation
used by both the router and libunit, with the chunk count computed so that a
peer-supplied size in the top of the 32-bit range cannot wrap to zero chunks
and be accepted.
</para>
</change>

<change type="change">
<para>
recycle connection structures through a per-engine freelist, embed the static
file context in the request, recycle static file buffer descriptors through a
thread-local freelist, and resolve the first eight routing variables and the
first sixteen request and response header fields from arrays embedded in the
request.  Each avoids or reuses an allocation on a per-connection or
per-request hot path.
</para>
</change>

<change type="change">
<para>
the WebAssembly component module no longer supports guest-initiated outbound
HTTP.  Serving Unit never needed it, and the feature that provided it linked
an entire TLS stack -- rustls, tokio-rustls, webpki-roots and rustls-webpki --
into the module.  A guest that only imports wasi:http/outgoing-handler, as the
wasi:http/proxy world requires, still instantiates and runs; an actual
outbound call now fails with HttpRequestDenied.
</para>
</change>

<change type="change">
<para>
update wasmtime from 36.0.12 to 47.0.2 in the WebAssembly component module,
keeping outbound HTTP denied and the TLS stack out of the dependency graph.
</para>
</change>

<change type="change">
<para>
the "if" option of a route "match" object, added in 1.33.0, is now
described in the OpenAPI specification and covered by the test suite.  A
request matches the step only when the condition is true; the condition
uses the same syntax as the "if" option of the access log: it is false
when it renders to an empty string, "0", "false", "null", or "undefined",
and true otherwise, and a leading "!" negates the result.  Variables test
for the presence of a value ("$arg_foo"), njs template strings express
comparisons and regular expressions
("`${uri == '/admin'}`", "`${/^\/admin/.test(uri)}`").
</para>
</change>

<change type="change">
<para>
build against OpenSSL 4.0, which constified the return types of the X509 name
accessors and deprecated X509_NAME_get_text_by_NID(); SNI certificate matching
and the "certificates" control API now read the certificate name through
X509_NAME_get_index_by_NID() and the entry accessors instead, and a new CI job
builds and runs the C test suite against OpenSSL 4.0 with deprecated APIs
disabled, so it fails on any new use of a deprecated function rather than only
on a removal.
</para>
</change>

<change type="change">
<para>
OpenSSL 1.1.1 or later is now required to build with "--openssl"; configure
fails with an explicit message on older releases.  Builds against OpenSSL 1.0.x
have not worked since 1.35.2, when the TLS module started to use
OpenSSL_version_num(); the unreachable pre-1.1.0 compatibility code is
removed.
</para>
</change>

<change type="bugfix">
<para>
close the engine port when a router worker thread exits.  Lowering
settings/listen_threads deletes engines, and the thread exit handler only
dropped a port reference, and the runtime held another, so nothing ever
called nxt_port_close(): every deleted engine leaked its socket pair and its
queue descriptor, three per engine, for the life of the router.  The count
only ever rose, so a configuration that raises and lowers the thread count
walked the router towards its descriptor limit.
</para>
</change>

<change type="bugfix">
<para>
close the descriptors a forged or malformed port message leaves behind.  A
message carries up to two descriptors whatever its type normally uses, and
the dispatcher reclaims nothing once a handler has returned.  The main
process kept the second descriptor on the whoami path, the router lost both
when a conf data message named no reply port, and a PROCESS_READY naming an
unknown process closed neither.  A peer that is already compromised could
exhaust the descriptor table of the receiving process -- the main process on
the whoami path -- by repeating any of them.
</para>
</change>

<change type="bugfix">
<para>
bound the log file slot a CHANGE_FILE message claims, and stop leaking the
descriptor it carries.  The slot index arrived from a peer and was used to
walk the runtime's log file list without a range check, and a message whose
payload was shorter than the index was read past its end.  The rejection
paths now close what the message attached, and nxt_file_redirect() releases
the descriptor when dup2() fails rather than only on success -- which also
ends a leak of one descriptor per unrotatable log file on every SIGUSR1.
</para>
</change>

<change type="bugfix">
<para>
a NEW_PORT or PROCESS_READY message carrying a shared-memory queue shorter
than the queue type faulted the receiving process (main, the prototype, or the
router) with SIGBUS on first access; the object size is now checked before
mapping, a short queue is refused with a warning, and the router no longer
leaks the descriptor of a queue it refuses.
</para>
</change>

<change type="bugfix">
<para>
a NEW_PORT message announcing a port that already existed, or one that could
not be created, left an uninitialized stack value behind; the controller
dereferenced it unconditionally on every NEW_PORT, and main and the router
relied on a NULL check that could not catch non-NULL garbage.  The result is
now cleared explicitly whenever no port is created.
</para>
</change>

<change type="bugfix">
<para>
close the file descriptors a NEW_PORT or MMAP message left behind instead of
leaking them.  The discovery and application prototype processes kept every
port queue descriptor open for the life of the process, the main process kept
the queue descriptor of any port it had not just created, the router lost it
on ports it does not map a queue for, the controller never closed it, and the
mmap handler leaked its second descriptor on every path and returned closing
nothing at all when the first was missing; a message type with no handler also
went uncollected.  A NEW_PORT for
a port that already has a queue now refuses the new descriptor instead of
mapping it over the live queue and leaking the mapping it replaced, which had
let a repeated announcement re-point an established port at memory of the
sender's choosing.
</para>
</change>

<change type="bugfix">
<para>
session tickets were silently disabled when Unit was built against an OpenSSL
3.x library compiled with the deprecated 3.0 API removed
(OPENSSL_NO_DEPRECATED_3_0), because the feature guard tested for a control
macro that is itself declared only under that API and so was never defined;
the session ticket key callback now registers through
SSL_CTX_set_tlsext_ticket_key_evp_cb() and keys an EVP_MAC context on OpenSSL
3.0 and later, independent of the deprecated HMAC_CTX API.
</para>
</change>

<change type="bugfix">
<para>
stop building OpenTelemetry span attributes for spans the sampler has
already dropped.  set_attribute() on a non-recording span discards its
argument, but the argument was built first -- the sprintf round-trips, the
pool allocations and two owned strings per attribute -- and nothing asked
whether the span was recording.  Lowering sampling_ratio therefore bought
back the exporter and nothing on the request path.  The sampler's verdict is
now read once when the span is created and the attribute stages consult it,
which removes most of telemetry's request-path cost at a low sampling
ratio.  Context propagation is deliberately outside that check: the
traceparent must reach the peer and the application whatever the sampling
decision was.
</para>
</change>

<change type="bugfix">
<para>
do not attach the traceparent to an OpenTelemetry span as a "Unit Attribute"
event.  It duplicated the trace and span identifiers the span context
already carries, nothing consumed it, and it was added twice on a request
that took the error path with a trace it had started itself, because the
error path re-runs header propagation.
</para>
</change>

<change type="bugfix">
<para>
OpenTelemetry spans were lost at the edges of a request's life.  A request
that ended before its span was collected -- an error path, a dropped
connection -- left the span allocated and never exported it; the span is now
tied to the memory pool that holds it and is exported with an error status
when that pool is released, so an abandoned request appears in the trace
instead of vanishing.
</para>
</change>

<change type="bugfix">
<para>
flush the OpenTelemetry span pipeline before the router exits.  Exit and
reload discarded the pipeline with up to 4096 spans still queued, losing every
span produced since the last export.  The pipeline is now flushed with a 2
second bound, so a slow or unreachable collector delays shutdown by at most
that long rather than either hanging it or costing the queued spans.
</para>
</change>

<change type="bugfix">
<para>
rebuild the OpenTelemetry exporter only when the telemetry settings have
changed.  Every configuration change rebuilt it, so adding a route or changing
an application stalled reconfiguration against an unreachable collector even
though it did not touch "/config/settings/telemetry".  The exporter is now
rebuilt only when the endpoint, protocol, sampling ratio or batch size
actually differs from the running one.
</para>
</change>

<change type="change">
<para>
the OpenTelemetry span attributes "http.response.status_code" and
"http.request.body.size" are now emitted as OTLP integer values, as the HTTP
semantic conventions specify, instead of strings.  A collector or query that
matches these attributes as strings must be adapted; one that reads them as
integers, or that relied on the convention rather than on Unit's previous
output, needs no change.
</para>
</change>

<change type="change">
<para>
the work a traced request does on the request path is reduced: attribute keys
are no longer copied onto the heap per request, the two attributes above no
longer round-trip through strings, and a span stage now crosses into the
tracer once instead of once per attribute.  Together with the sampling fix
above, the telemetry overhead of a request measured 27% lower at
"sampling_ratio" 1.0 and 53% lower at 0.1.
</para>
</change>

<change type="change">
<para>
the Java module now bundles Apache Tomcat 9.0.121 (from 9.0.110) and
ClassGraph 4.8.186 (from 4.8.181).  The upstream Tomcat 9.0.121 release fixes
CVE-2026-66299 (unbounded buffer for undelivered WebSocket messages, a memory
exhaustion by a slow client), CVE-2026-73180, CVE-2026-68569, CVE-2026-66422
and CVE-2026-65927.  FreeUnit bundles only the servlet, JSP and EL API jars,
Jasper and the Tomcat utility jars -- not catalina, the WebSocket
implementation or the example webapps -- so the bump keeps the jars it ships
on a supported release.
</para>
</change>

</changes>


<changes apply="unit-php unit-php8.3 unit-php8.4 unit-php8.5
                unit-python unit-python2.7
                unit-python3.8 unit-python3.9 unit-python3.10 unit-python3.11
                unit-python3.12 unit-python3.13 unit-python3.14
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc11 unit-jsc17 unit-jsc18
                unit-jsc19 unit-jsc20 unit-jsc21
                unit-wasm"
         ver="1.36.1" rev="1"
         date="2026-08-28" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change>
<para>
FreeUnit updated to 1.36.1.
</para>
</change>

</changes>


<changes apply="unit"
         ver="1.36.0" rev="1"
         date="2026-07-16" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change type="feature">
<para>
upgrade the OpenTelemetry Rust crates from 0.24 to 0.32 and rewrite the
OTLP trace exporter on the stable, dedicated-thread batch span processor
(blocking reqwest client, no tokio runtime).
</para>
</change>

<change type="feature">
<para>
W3C traceparent header inheritance now correctly propagates the upstream
sampling decision; the sampler wraps TraceIdRatioBased inside ParentBased
so that remote parent trace flags are honoured.
</para>
</change>

<change type="feature">
<para>
add fake_otlp mock OTLP collector (test/fake_otlp/) and the test_otel.py
test suite covering span export, traceparent propagation, sampling, and
configuration validation over both the OTLP/HTTP and OTLP/gRPC transports.
</para>
</change>

<change type="feature">
<para>
reintroduce OTLP/gRPC trace export. Both transports are compiled into
every "--otel" build and the "settings/telemetry/protocol" option selects
"http" (OTLP/HTTP) or "grpc" (OTLP/gRPC) at runtime, as in upstream Unit.
gRPC uses tonic over a small tokio runtime; v1 is plaintext h2c only (no
TLS to the collector, like the HTTP path).
</para>
</change>

<change type="change">
<para>
rename the default OTel service name from "NGINX Unit" to "FreeUnit".
</para>
</change>

<change type="bugfix">
<para>
add validation bounds for "batch_size" (1-65536) and "sampling_ratio"
(0-1) in the OpenTelemetry configuration; previously invalid values were
silently rejected without an error message.
</para>
</change>

<change type="bugfix">
<para>
replace eprintln! error output in the OTel Rust layer with the C log
callback (nxt_otel_log_cb), so errors flow through the standard Unit
logging infrastructure.
</para>
</change>

<change type="bugfix">
<para>
remove unreachable Protocol::HttpJson dead-code arm from the OTel
exporter; only HttpBinary was ever selected.
</para>
</change>

<change type="bugfix">
<para>
fix a libunit port-fd double-close race in the Go language module:
getUnixConn() wrapped the raw shared-port descriptor (which net.FileConn
dups) and a deferred Close() closed the original descriptor number while
libunit still owned it, an intermittent double-close that raised an EBADF
alert or, worse, closed an unrelated reused live descriptor. Go now dup(2)s
the descriptor and wraps only the dup, so libunit remains the sole owner
and closer; libunit additionally holds a port reference across the
out-of-mutex add_port callback window so a concurrent destroy cannot free
the port mid-callback.
</para>
</change>

<change type="bugfix">
<para>
fix an ineffective truncation guard when building the cgroup.procs
pathname: snprintf()'s return value overwrote the original directory length
before the bounds comparison, so a long cgroup directory path could
overflow the check. The append now preserves the original length and fails
with ENAMETOOLONG when "/cgroup.procs" plus the trailing NUL cannot fit.
</para>
</change>

<change type="bugfix">
<para>
fail cgroup setup when the per-process pool allocation that caches the
created directory for cleanup fails; the directory is removed and setup
errors out instead of moving the process into a cgroup that could never be
cleaned up -- a cgroup directory leak under memory pressure.
</para>
</change>

<change type="bugfix">
<para>
keep downstream keepalive disabled after a duplicate or invalid upstream
"Content-Length"; a clean upstream close no longer re-enables it, so an
HTTP/1.1 client cannot reuse the connection past the re-framed response. The
complete body still ends with its terminal chunk -- body truncation is now
tracked separately from the inconsistent flag.
</para>
</change>

<change type="change">
<para>
upgrade contrib njs to 1.0.0.
</para>
</change>

<change type="change">
<para>
the Debian packages are rebranded to "freeunit": the package set
(freeunit, freeunit-dev, freeunit-php8.x, freeunit-python3.x,
freeunit-perl, freeunit-ruby, freeunit-go, freeunit-wasm and the
freeunit-jscNN Java modules), the
daemon binary (/usr/sbin/freeunitd), the control socket
(control.freeunit.sock), the state directory (/var/lib/freeunit), the log
(/var/log/freeunit.log) and the systemd/init service are all renamed. Each
package Provides/Conflicts/Replaces its upstream "unit" counterpart, so
installing freeunit cleanly takes over a stock "unit" install; the two are
deliberately not co-installable. The compiled-in "Unit" identity, the
unitc/setup-unit tools and the development headers keep their upstream
names by design.
</para>
</change>

</changes>


<changes apply="unit-php unit-php8.3 unit-php8.4 unit-php8.5 unit-python unit-python3.13 unit-python3.14
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc17 unit-jsc21
                unit-wasm"
         ver="1.36.0" rev="1"
         date="2026-07-16" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change>
<para>
FreeUnit updated to 1.36.0.
</para>
</change>

</changes>


<changes apply="unit"
         ver="1.35.5" rev="1"
         date="2026-05-29" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change type="feature">
<para>
automatically convert chunked request bodies to Content-Length
when forwarding to upstream servers via proxy action.
This enables compatibility with backends that do not support
Transfer-Encoding: chunked (e.g., Gitea, servers requiring
Content-Length).
</para>
</change>

<change type="feature">
<para>
chunk extension support in chunked body parser (RFC 9112 §7.1).
</para>
</change>

<change type="change">
<para>
chunked_transform feature is no longer experimental.
Chunked request bodies can be accepted and transparently converted
to Content-Length via configuration:
{ "settings": { "http": { "chunked_transform": true } } }
</para>
</change>

<change type="bugfix">
<para>
fix TLS library busy-loop on peer-initiated close in SSL_write
when connection is aborted by remote peer; prevents high CPU usage
and ensures proper connection cleanup.
</para>
</change>

<change type="bugfix">
<para>
fix mem-pool retain leak in cert/script-store IPC paths
(router side) and fd/buffer leaks in cert/script/socket/access-log
reply paths and the controller config-store path (main process side);
all reachable when nxt_port_msg_alloc fails inside the port machinery.
</para>
</change>

<change type="bugfix">
<para>
fix isolation mount comparator ordering to ensure procfs mounts
before tmpfs before bind/lang-deps; avoids flaky ENOENT on procfs
mount in rootfs isolation with automount.
</para>
</change>

<change type="change">
<para>
upgrade contrib njs to 0.9.8.
</para>
</change>

<change type="feature">
<para>
add unfreeze-sync.sh script for automated migration of issues
from nginx/unit to freeunitorg/freeunit with label mapping,
deduplication, and dry-run preview support.
</para>
</change>

</changes>


<changes apply="unit-php unit-php8.3 unit-php8.4 unit-php8.5 unit-python unit-python3.13 unit-python3.14
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc17 unit-jsc21
                unit-wasm"
         ver="1.35.5" rev="1"
         date="2026-05-29" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change>
<para>
FreeUnit updated to 1.35.5.
</para>
</change>

<change type="feature">
<para>
Debian trixie packages are now built for php8.3, php8.4, php8.5 and python3.13.
</para>
</change>

</changes>


<changes apply="unit"
         ver="1.35.4" rev="1"
         date="2026-04-30" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change type="security">
<para>
all official Docker images now ship /usr/share/unit/seccomp-no-af-alg.json,
a seccomp profile blocking socket(AF_ALG, ...) (domain 38), mitigating
CVE-2026-31431 — local privilege escalation (CVSS 7.8) via the Linux kernel
algif_aead component. Apply from repo root:
docker run --security-opt seccomp=pkg/docker/seccomp-no-af-alg.json.
</para>
</change>

<change type="bugfix">
<para>
fix router process CPU spin and connection hang under port scanning load;
CLOSE-WAIT sockets are now cleaned up properly on client FIN, idle
connection queue iteration fixed, systemd file descriptor limit increased
to 65535.
</para>
</change>

<change type="change">
<para>
default HTTP keep-alive idle_timeout reduced from 180 to 30 seconds.
</para>
</change>

<change type="change">
<para>
upgrade wasmtime to 43.0.1.
</para>
</change>

<change type="bugfix">
<para>
replace removed cgi module with email.parser in Python upload test fixture;
fixes test suite compatibility with Python 3.13.
</para>
</change>

</changes>

<changes apply="unit-php unit-python unit-python3.13
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc17 unit-jsc21
                unit-wasm"
         ver="1.35.4" rev="1"
         date="2026-04-30" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change>
<para>
FreeUnit updated to 1.35.4.
</para>
</change>

</changes>


<changes apply="unit"
         ver="1.35.3" rev="1"
         date="2026-04-07" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change type="feature">
<para>
migrate contrib package mirror from packages.nginx.org to packages.freeunit.org;
all dependency tarballs are now served from FreeUnit infrastructure.
</para>
</change>

<change type="feature">
<para>
upgrade contrib dependencies: njs 0.9.6, libunit-wasm 0.5.0, wasmtime 43.0.0,
wasi-sysroot 27.0.
</para>
</change>

<change type="feature">
<para>
replace EOL jsc11 Docker image with jsc17 and jsc21 based on Eclipse Temurin
(Ubuntu Noble).
</para>
</change>

<change type="feature">
<para>
split Docker CI into native AMD64 and ARM64 build jobs with a separate
manifest-merge step; eliminates QEMU emulation overhead.
</para>
</change>

<change type="change">
<para>
bump Rust toolchain to 1.94.1 in all Docker images.
</para>
</change>

<change type="change">
<para>
unitctl Dockerfile GPG key URL migrated to freeunit.org.
</para>
</change>

</changes>

<changes apply="unit-php unit-python
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc17 unit-jsc21
                unit-wasm"
         ver="1.35.3" rev="1"
         date="2026-04-07" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change>
<para>
FreeUnit updated to 1.35.3.
</para>
</change>

</changes>


<changes apply="unit"
         ver="1.35.2" rev="1"
         date="2026-04-04" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change type="bugfix">
<para>
fix graceful shutdown for PHP workers running in TrueAsync mode; workers now
exit cleanly when Unit delivers a quit signal instead of blocking indefinitely
in the event loop.
</para>
</change>

<change type="bugfix">
<para>
guard pre_request_init SAPI callback registration with NXT_PHP_PRE_REQUEST_INIT
to fix builds against PHP versions that do not expose this callback.
</para>
</change>

<change type="bugfix">
<para>
fix Ruby language module build in clang-ast workflow on debian:testing: resolve
Debian multiarch library path mismatch and suppress
-Wdefault-const-init-field-unsafe from clang 21 on Ruby 3.3 headers.
</para>
</change>

</changes>

<changes apply="unit-php"
         ver="1.35.2" rev="1"
         date="2026-04-04" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change>
<para>
FreeUnit updated to 1.35.2.
</para>
</change>

</changes>


<changes apply="unit"
         ver="1.35.1" rev="1"
         date="2026-04-03" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change type="feature">
<para>
Docker images for PHP 8.5, PHP 8.4, and Python 3.14 published to GitHub
Container Registry (GHCR) for linux/amd64 and linux/arm64.
</para>
</change>

<change type="feature">
<para>
switch all Docker base images from debian:bookworm to debian:trixie.
</para>
</change>

<change type="feature">
<para>
add PHP 8.5 pre_request_init SAPI callback support.
</para>
</change>

<change type="feature">
<para>
add PHP 8.4 and PHP 8.5 to CI test matrix.
</para>
</change>

<change type="feature">
<para>
build CI against OpenSSL 3.6.
</para>
</change>

<change type="change">
<para>
fork from nginx/unit as FreeUnit: community LTS continuation of the Unit
application server.
</para>
</change>

</changes>

<changes apply="unit-php unit-python
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc17 unit-jsc21
                unit-wasm"
         ver="1.35.1" rev="1"
         date="2026-04-03" time="00:00:00 +0000"
         packager="FreeUnit Packaging &lt;team@freeunit.org&gt;">

<change>
<para>
FreeUnit updated to 1.35.1.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10 unit-python3.11
                unit-python3.12 unit-python3.13
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18
                unit-jsc19 unit-jsc20 unit-jsc21
                unit-wasm"
         ver="1.35.0" rev="1"
         date="2025-09-03" time="18:00:00 +0000"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
NGINX Unit updated to 1.35.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.35.0" rev="1"
         date="2025-09-03" time="18:00:00 +0000"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change type="security">
<para>
fix missing websocket payload length validation in the Java language module
which could lead to Java language module processes consuming excess CPU.
(CVE-2025-1695).
</para>
</change>

<change type="change">
<para>
if building with njs, version 0.9.0 or later is now required.
</para>
</change>

<change type="feature">
<para>
HTTP compression.
</para>
</change>

<change type="feature">
<para>
PHP 8.5 compatibility.
</para>
</change>

<change type="feature">
<para>
Ruby 3.4 compatibility.
</para>
</change>

<change type="feature">
<para>
Django 5.x compatibility.
</para>
</change>

<change type="feature">
<para>
Python Litestar WebSockets compatibility.
</para>
</change>

<change type="feature">
<para>
GCC 15 compatibility.
</para>
</change>

<change type="bugfix">
<para>
set SERVER_PORT to the actual value.
</para>
</change>

<change type="bugfix">
<para>
fix issue in node.js with duplicate headers in response.
</para>
</change>

<change type="bugfix">
<para>
fix WebSockets with Firefox.
</para>
</change>

<change type="bugfix">
<para>
fix incorrect websocket payload length calculation in the Java language module.
</para>
</change>

<change type="bugfix">
<para>
fix instability issues due to OpenTelemetry (OTEL) support.
</para>
</change>

<change type="bugfix">
<para>
fix issues with building OpenTelemetry (OTEL) support on various platforms,
including macOS.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10 unit-python3.11
                unit-python3.12 unit-python3.13
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18
                unit-jsc19 unit-jsc20 unit-jsc21
                unit-wasm"
         ver="1.34.0" rev="1"
         date="2024-12-19" time="18:00:00 +0000"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
NGINX Unit updated to 1.34.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.34.0" rev="1"
         date="2024-12-19" time="18:00:00 +0000"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change type="feature">
<para>
initial OpenTelemetry (OTEL) support. (Disabled by default).
</para>
</change>

<change type="feature">
<para>
support for JSON formatted access logs.
</para>
</change>

<change type="bugfix">
<para>
tweak the Perl language module to avoid breaking scripts in some
circumstances.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10 unit-python3.11
                unit-python3.12
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18
                unit-jsc19 unit-jsc20 unit-jsc21
                unit-wasm"
         ver="1.33.0" rev="1"
         date="2024-09-17" time="18:00:00 +0000"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
NGINX Unit updated to 1.33.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.33.0" rev="1"
         date="2024-09-17" time="18:00:00 +0000"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change type="feature">
<para>
make the number of router threads configurable.
</para>
</change>

<change type="feature">
<para>
make the listen(2) backlog configurable.
</para>
</change>

<change type="feature">
<para>
add Python application factory support.
</para>
</change>

<change type="feature">
<para>
add experimental chunked request body support. (Disabled by default).
</para>
</change>

<change type="feature">
<para>
add fuzzing via oss-fuzz.
</para>
</change>

<change type="feature">
<para>
add "if" option to the "match" object.
</para>
</change>

<change type="feature">
<para>
show list of loaded language modules in the /status endpoint.
</para>
</change>

<change type="feature">
<para>
Unit ships with a new Rust based CLI application "unitctl".
</para>
</change>

<change type="feature">
<para>
the wasm-wasi-component language module now inherits the processes
environment.
</para>
</change>

<change type="change">
<para>
under systemd unit runs in forking mode (once again).
</para>
</change>

<change type="change">
<para>
if building with njs, version 0.8.3 or later is now required.
</para>
</change>

<change type="change">
<para>
Unit now builds with -std=gnu11 (C11 with GNU extensions).
</para>
</change>

<change type="change">
<para>
Unit now creates the full directory path for the PID file and control socket.
</para>
</change>

<change type="change">
<para>
build system improvements, including pretty printing the make output and
enabling various make variables to influence the build process (see:
make help).
</para>
</change>

<change type="change">
<para>
better detection of available runnable CPUs on Linux.
</para>
</change>

<change type="change">
<para>
default listen(2) backlog on Linux now defaults to Kernel default.
</para>
</change>

<change type="bugfix">
<para>
don't modify REQUEST_URI.
</para>
</change>

<change type="bugfix">
<para>
fix a crash when interrupting a download via a proxy.
</para>
</change>

<change type="bugfix">
<para>
wasm-wasi-component application process hangs after receiving restart signal
from the control endpoint.
</para>
</change>

<change type="bugfix">
<para>
njs variables accessed with a JS template literal should not be cacheable.
</para>
</change>

<change type="bugfix">
<para>
properly handle deleting arrays of certificates.
</para>
</change>

<change type="bugfix">
<para>
don't create the $runstatedir directory which triggered an Alpine packaging
error.
</para>
</change>

</changes>


<changes apply="unit-jsc21" ver="1.32.0" rev="1"
         date="2023-10-17" time="16:00:00 -0700"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
Initial release of Java 21 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-python3.12" ver="1.32.0" rev="1"
         date="2023-10-17" time="16:00:00 -0700"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
Initial release of Python 3.12 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10 unit-python3.11
                unit-python3.12
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18
                unit-jsc19 unit-jsc20 unit-jsc21
                unit-wasm"
         ver="1.32.0" rev="1"
         date="2024-02-27" time="18:00:00 +0000"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
NGINX Unit updated to 1.32.0.
</para>
</change>


</changes>


<changes apply="unit" ver="1.32.0" rev="1"
         date="2024-02-27" time="18:00:00 +0000"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change type="feature">
<para>
WebAssembly Components using WASI interfaces defined in wasi:http/proxy@0.2.0.
</para>
</change>

<change type="feature">
<para>
conditional access logging.
</para>
</change>

<change type="feature">
<para>
njs variables access.
</para>
</change>

<change type="feature">
<para>
$request_id variable contains a string that is formed using random data and
can be used as a unique request identifier.
</para>
</change>

<change type="feature">
<para>
options to set control socket permissions.
</para>
</change>

<change type="feature">
<para>
Ruby arrays in response headers, improving compatibility with Rack v3.0.
</para>
</change>

<change type="feature">
<para>
Python bytearray response bodies for ASGI applications.
</para>
</change>

<change type="bugfix">
<para>
router could crash while sending large files. Thanks to rustedsword.
</para>
</change>

<change type="bugfix">
<para>
serving static files from a network filesystem could lead to error.
</para>
</change>

<change type="bugfix">
<para>
"uidmap" and "gidmap" isolation options validation.
</para>
</change>

<change type="bugfix">
<para>
abstract UNIX socket name could be corrupted during configuration validation.
Thanks to Alejandro Colomar.
</para>
</change>

<change type="bugfix">
<para>
HTTP header field value encoding could be misinterpreted in Python module.
</para>
</change>

<change type="bugfix">
<para>
Node.js http.createServer() accepts and ignores the "options" argument,
improving compatibility with strapi applications, among others.
</para>
</change>

<change type="bugfix">
<para>
ServerRequest.flushHeaders() implemented in Node.js module to make it compatible
with Next.js.
</para>
</change>

<change type="bugfix">
<para>
ServerRequest.httpVersion variable format in Node.js module.
</para>
</change>

<change type="bugfix">
<para>
Node.js module handles standard library imports prefixed with "node:", making it
possible to run newer Nuxt applications, among others.
</para>
</change>

<change type="bugfix">
<para>
Node.js tarball location changed to avoid build/install errors.
</para>
</change>

<change type="bugfix">
<para>
Go module sets environment variables necessary for building on macOS/arm64
systems.
</para>
</change>

</changes>

<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10 unit-python3.11
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18
                unit-jsc19 unit-jsc20
                unit-wasm"
         ver="1.31.1" rev="1"
         date="2023-10-19" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
NGINX Unit updated to 1.31.1.
</para>
</change>

</changes>


<changes apply="unit" ver="1.31.1" rev="1"
         date="2023-10-19" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change type="feature">
<para>
allow to set the HTTP response status in Wasm module.
</para>
</change>

<change type="feature">
<para>
allow uploads larger than 4GiB in Wasm module.
</para>
</change>

<change type="bugfix">
<para>
application process could crash while rewriting URLs with query strings.
</para>
</change>

<change type="bugfix">
<para>
requests larger than about 64MiB could cause error in Wasm module.
</para>
</change>

<change type="bugfix">
<para>
when using many headers in Java module some of them could be
corrupted due to memory realocation issue.
</para>
</change>

<change type="bugfix">
<para>
ServerRequest.destroy() implemented in Node.js module to make it compatible
with some frameworks that might use it.
</para>
</change>

<change type="bugfix">
<para>
chunk argument of ServerResponse.write() can now be a Uint8Array to improve
compatibility with Node.js 15.0.0 and above.
</para>
</change>

<change type="bugfix">
<para>
Node.JS unit-http NPM module now has appropriate default paths for macOS/arm64
systems.
</para>
</change>

<change type="bugfix">
<para>
build on musl libc with clang.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10 unit-python3.11
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18
                unit-jsc19 unit-jsc20
                unit-wasm"
         ver="1.31.0" rev="1"
         date="2023-08-31" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
NGINX Unit updated to 1.31.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.31.0" rev="1"
         date="2023-08-31" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change type="change">
<para>
if building with njs, version 0.8.0 or later is now required.
</para>
</change>

<change type="feature">
<para>
technology preview of WebAssembly application module.
</para>
</change>

<change type="feature">
<para>
"response_headers" option to manage headers in the action and fallback.
</para>
</change>

<change type="feature">
<para>
HTTP response header variables.
</para>
</change>

<change type="feature">
<para>
ASGI lifespan state support. Thanks to synodriver.
</para>
</change>

<change type="bugfix">
<para>
ensure that $uri variable is not cached.
</para>
</change>

<change type="bugfix">
<para>
deprecated options were unavailable.
</para>
</change>

<change type="bugfix">
<para>
ASGI applications inaccessible over IPv6.
</para>
</change>

</changes>


<changes apply="unit-wasm" ver="1.31.0" rev="1"
         date="2023-08-14" time="15:00:00 -0700"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
Initial release of WASM module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10 unit-python3.11
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18
                unit-jsc19 unit-jsc20"
         ver="1.30.0" rev="1"
         date="2023-05-10" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
NGINX Unit updated to 1.30.0.
</para>
</change>

</changes>


<changes apply="unit-jsc20" ver="1.30.0" rev="1"
         date="2023-05-03" time="15:00:00 -0700"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
Initial release of Java 20 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit" ver="1.30.0" rev="1"
         date="2023-05-10" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change type="change">
<para>
remove Unix domain listen sockets upon reconfiguration.
</para>
</change>

<change type="feature">
<para>
basic URI rewrite support.
</para>
</change>

<change type="feature">
<para>
njs loadable modules support.
</para>
</change>

<change type="feature">
<para>
per-application logging.
</para>
</change>

<change type="feature">
<para>
conditional logging of route selection.
</para>
</change>

<change type="feature">
<para>
support the keys API on the request objects in njs.
</para>
</change>

<change type="feature">
<para>
default values for 'make install' pathnames such as prefix;
this allows to './configure &amp;&amp; make &amp;&amp; sudo make install'.
</para>
</change>

<change type="feature">
<para>
"server_version" setting to omit the version token from "Server" header field.
</para>
</change>

<change type="bugfix">
<para>
request header field values could be corrupted in some cases; the bug had
appeared in 1.29.0.
</para>
</change>

<change type="bugfix">
<para>
PHP error handling (added missing 403 and 404 errors).
</para>
</change>

<change type="bugfix">
<para>
Perl applications crash on second responder call.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10 unit-python3.11
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18
                unit-jsc19"
         ver="1.29.1" rev="1"
         date="2023-02-28" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
NGINX Unit updated to 1.29.1.
</para>
</change>

</changes>


<changes apply="unit" ver="1.29.1" rev="1"
         date="2023-02-28" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change type="bugfix">
<para>
stop creating world-writeable directories.
</para>
</change>

<change type="bugfix">
<para>
memory leak related to njs.
</para>
</change>

<change type="bugfix">
<para>
path parsing in PHP applications.
</para>
</change>

<change type="bugfix">
<para>
enabled UTF-8 for Python config by default to avoid applications failing
in some cases.
</para>
</change>

<change type="bugfix">
<para>
using asyncio.get_running_loop() instead of asyncio.get_event_loop()
when it's available to prevent errors in some Python ASGI applications.
</para>
</change>

<change type="bugfix">
<para>
applications that make use of various low level APIs such as pthreads could
fail to work correctly.
</para>
</change>

<change type="bugfix">
<para>
websocket endianness detection for obscure operating systems.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10 unit-python3.11
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18
                unit-jsc19"
         ver="1.29.0" rev="1"
         date="2022-12-15" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
NGINX Unit updated to 1.29.0.
</para>
</change>

</changes>


<changes apply="unit-jsc19" ver="1.29.0" rev="1"
         date="2022-11-18" time="15:00:00 +0400"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
Initial release of Java 19 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-python3.11" ver="1.29.0" rev="1"
         date="2022-12-15" time="08:00:00 -0800"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
Initial release of Python 3.11 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit" ver="1.29.0" rev="1"
         date="2022-12-15" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change type="change">
<para>
removed $uri auto-append for "share" when loading configuration.
</para>
</change>

<change type="change">
<para>
prefer system crypto policy instead of hardcoding a default.
</para>
</change>

<change type="feature">
<para>
njs support with the basic syntax of JS template literals.
</para>
</change>

<change type="feature">
<para>
support per-application cgroups on Linux.
</para>
</change>

<change type="feature">
<para>
the $request_time variable contains the request processing time.
</para>
</change>

<change type="feature">
<para>
"prefix" option in Python applications to set WSGI "SCRIPT_NAME"
and ASGI root-path variables.
</para>
</change>

<change type="feature">
<para>
compatibility with Python 3.11.
</para>
</change>

<change type="feature">
<para>
compatibility with OpenSSL 3.
</para>
</change>

<change type="feature">
<para>
compatibility with PHP 8.2.
</para>
</change>

<change type="feature">
<para>
compatibility with Node.js 19.0.
</para>
</change>

<change type="feature">
<para>
Ruby Rack v3 support.
</para>
</change>

<change type="bugfix">
<para>
fix error in connection statistics when using proxy.
</para>
</change>

<change type="bugfix">
<para>
fix HTTP cookie parsing when the value contains an equals sign.
</para>
</change>

<change type="bugfix">
<para>
PHP directory URLs without a trailing '/' would give a 503 error (fixed with
a 301 re-direct).
</para>
</change>

<change type="bugfix">
<para>
missing error checks in the C API.
</para>
</change>

<change type="bugfix">
<para>
report the regex status in configure summary.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18"
         ver="1.28.0" rev="1"
         date="2022-09-13" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change>
<para>
NGINX Unit updated to 1.28.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.28.0" rev="1"
         date="2022-09-13" time="18:00:00 +0300"
         packager="Nginx Packaging &lt;nginx-packaging@f5.com&gt;">

<change type="change">
<para>
increased the applications' startup timeout.
</para>
</change>

<change type="change">
<para>
disallowed abstract Unix domain socket syntax in non-Linux systems.
</para>
</change>

<change type="feature">
<para>
basic statistics API.
</para>
</change>

<change type="feature">
<para>
customizable access log format.
</para>
</change>

<change type="feature">
<para>
more HTTP variables support.
</para>
</change>

<change type="feature">
<para>
forwarded header to replace client address and protocol.
</para>
</change>

<change type="feature">
<para>
ability to get dynamic variables.
</para>
</change>

<change type="feature">
<para>
support for abstract Unix sockets.
</para>
</change>

<change type="feature">
<para>
support for Unix sockets in address matching.
</para>
</change>

<change type="feature">
<para>
the $dollar variable translates to a literal "$" during variable substitution.
</para>
</change>

<change type="bugfix">
<para>
router process could crash if index file didn't contain an extension.
</para>
</change>

<change type="bugfix">
<para>
force SCRIPT_NAME in Ruby to always be an empty string.
</para>
</change>

<change type="bugfix">
<para>
when isolated PID numbers reach the prototype process host PID,
the prototype crashed.
</para>
</change>

<change type="bugfix">
<para>
the Ruby application process could crash on SIGTERM.
</para>
</change>

<change type="bugfix">
<para>
the Ruby application process could crash on SIGINT.
</para>
</change>

<change type="bugfix">
<para>
mutex leak in the C API.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18"
         ver="1.27.0" rev="1"
         date="2022-06-02" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.27.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.27.0" rev="1"
         date="2022-06-02" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
ability to specify a custom index file name when serving static files.
</para>
</change>

<change type="feature">
<para>
variables support in the "location" option of the "return" action.
</para>
</change>

<change type="feature">
<para>
support empty strings in the "location" option of the "return" action.
</para>
</change>

<change type="feature">
<para>
added a new variable, $request_uri, that includes both the path and the query
parts as per RFC 3986, sections 3-4.
</para>
</change>

<change type="feature">
<para>
Ruby Rack environment parameter "SCRIPT_NAME" support.
</para>
</change>

<change type="feature">
<para>
compatibility with GCC 12.
</para>
</change>

<change type="bugfix">
<para>
Ruby Sinatra applications don't work without custom logging.
</para>
</change>

<change type="bugfix">
<para>
the controller process could crash when a chain of more than four
certificates was uploaded.
</para>
</change>

<change type="bugfix">
<para>
some Perl applications failed to process the request body, notably with Plack.
</para>
</change>

<change type="bugfix">
<para>
some Spring Boot applications failed to start, notably with Grails.
</para>
</change>

<change type="bugfix">
<para>
incorrect Python protocol auto detection (ASGI or WSGI) for native callable
object, notably with Falcon.
</para>
</change>

<change type="bugfix">
<para>
ECMAScript modules did not work with the recent Node.js versions.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9 unit-python3.10
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17 unit-jsc18"
         ver="1.26.1" rev="1"
         date="2021-12-02" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.26.1.
</para>
</change>

</changes>


<changes apply="unit" ver="1.26.1" rev="1"
         date="2021-12-02" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="bugfix">
<para>
occasionally, the Unit daemon was unable to fully terminate; the bug had
appeared in 1.26.0.
</para>
</change>

<change type="bugfix">
<para>
a prototype process could crash on an application process exit; the bug had
appeared in 1.26.0.
</para>
</change>

<change type="bugfix">
<para>
the router process crashed on reconfiguration if "access_log" was configured
without listeners.
</para>
</change>

<change type="bugfix">
<para>
a segmentation fault occurred in the PHP module if chdir() or
fastcgi_finish_request() was called in the OPcache preloading script.
</para>
</change>

<change type="bugfix">
<para>
fatal errors on DragonFly BSD; the bug had appeared in 1.26.0.
</para>
</change>

</changes>


<changes apply="unit-jsc18" ver="1.26.0" rev="1"
         date="2021-11-18" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Java 18 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-python3.10" ver="1.26.0" rev="1"
         date="2021-11-18" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Python 3.10 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17"
         ver="1.26.0" rev="1"
         date="2021-11-18" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.26.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.26.0" rev="1"
         date="2021-11-18" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
the "share" option now specifies the entire path to the files it serves,
rather than a document root directory to be prepended to the request URI.
</para>
</change>

<change type="feature">
<para>
automatic adjustment of existing configurations to the new "share" behavior
when updating from previous versions.
</para>
</change>

<change type="feature">
<para>
variables support in the "share" option.
</para>
</change>

<change type="feature">
<para>
multiple paths in the "share" option.
</para>
</change>

<change type="feature">
<para>
variables support in the "chroot" option.
</para>
</change>

<change type="feature">
<para>
PHP opcache is shared between application processes.
</para>
</change>

<change type="feature">
<para>
request routing by the query string.
</para>
</change>

<change type="bugfix">
<para>
the router and app processes could crash when the requests limit was reached
by asynchronous or multithreaded apps.
</para>
</change>

<change type="bugfix">
<para>
established WebSocket connections could stop reading frames from the client
after the corresponding listener had been reconfigured.
</para>
</change>

<change type="bugfix">
<para>
fixed building with glibc 2.34, notably Fedora 35.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15 unit-jsc16 unit-jsc17"
         ver="1.25.0" rev="1"
         date="2021-08-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.25.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.25.0" rev="1"
         date="2021-08-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
client IP address replacement from a specified HTTP header field.
</para>
</change>

<change type="feature">
<para>
TLS sessions cache.
</para>
</change>

<change type="feature">
<para>
TLS session tickets.
</para>
</change>

<change type="feature">
<para>
application restart control.
</para>
</change>

<change type="feature">
<para>
process and thread lifecycle hooks in Ruby.
</para>
</change>

<change type="bugfix">
<para>
the router process could crash on TLS connection open when multiple listeners
with TLS certificates were configured; the bug had appeared in 1.23.0.
</para>
</change>

<change type="bugfix">
<para>
TLS connections were rejected for configurations with multiple certificate
bundles in a listener if the client did not use SNI.
</para>
</change>

<change type="bugfix">
<para>
the router process could crash with frequent multithreaded application
reconfiguration.
</para>
</change>

<change type="bugfix">
<para>
compatibility issues with some Python ASGI apps, notably based on the Starlette
framework.
</para>
</change>

<change type="bugfix">
<para>
a descriptor and memory leak occurred in the router process when an app process
stopped or crashed.
</para>
</change>

<change type="bugfix">
<para>
the controller or router process could crash if the configuration contained
a full-form IPv6 in a listener address.
</para>
</change>

<change type="bugfix">
<para>
the router process crashed when a request was passed to an empty "routes"
or "upstreams" using a variable "pass" option.
</para>
</change>

<change type="bugfix">
<para>
the router process crashed while matching a request to an empty array of source
or destination address patterns.
</para>
</change>

</changes>


<changes apply="unit-jsc17" ver="1.24.0" rev="1"
         date="2021-05-27" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Java 17 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-jsc16" ver="1.24.0" rev="1"
         date="2021-05-27" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Java 16 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15"
         ver="1.24.0" rev="1"
         date="2021-05-27" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.24.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.24.0" rev="1"
         date="2021-05-27" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
PHP added to the default MIME type list.
</para>
</change>

<change type="feature">
<para>
arbitrary configuration of TLS connections via OpenSSL commands.
</para>
</change>

<change type="feature">
<para>
the ability to limit static file serving by MIME types.
</para>
</change>

<change type="feature">
<para>
support for chrooting, rejecting symlinks, and rejecting mount
point traversal on a per-request basis when serving static files.
</para>
</change>

<change type="feature">
<para>
a loader for automatically overriding the "http" and "websocket" modules in
Node.js.
</para>
</change>

<change type="feature">
<para>
multiple "targets" in Python applications.
</para>
</change>

<change type="feature">
<para>
compatibility with Ruby 3.0.
</para>
</change>

<change type="bugfix">
<para>
the router process could crash while closing a TLS connection.
</para>
</change>

<change type="bugfix">
<para>
a segmentation fault might have occurred in the PHP module if
fastcgi_finish_request() was used with the "auto_globals_jit" option enabled.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8 unit-python3.9
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15"
         ver="1.23.0" rev="1"
         date="2021-03-25" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.23.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.23.0" rev="1"
         date="2021-03-25" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
support for multiple certificate bundles on a listener via the Server Name
Indication (SNI) TLS extension.
</para>
</change>

<change type="feature">
<para>
"--mandir" ./configure option to specify the directory for man page
installation.
</para>
</change>

<change type="bugfix">
<para>
the router process could crash on premature TLS connection close; the bug had
appeared in 1.17.0.
</para>
</change>

<change type="bugfix">
<para>
a connection leak occurred on premature TLS connection close; the bug had
appeared in 1.6.
</para>
</change>

<change type="bugfix">
<para>
a descriptor and memory leak occurred in the router process when processing
small WebSocket frames from a client; the bug had appeared in 1.19.0.
</para>
</change>

<change type="bugfix">
<para>
a descriptor leak occurred in the router process when removing or
reconfiguring an application; the bug had appeared in 1.19.0.
</para>
</change>

<change type="bugfix">
<para>
persistent storage of certificates might've not worked with some filesystems in
Linux, and all uploaded certificate bundles were forgotten after restart.
</para>
</change>

<change type="bugfix">
<para>
the controller process could crash while requesting information about a
certificate with a non-DNS SAN entry.
</para>
</change>

<change type="bugfix">
<para>
the controller process could crash on manipulations with a certificate
containing a SAN and no standard name attributes in subject or issuer.
</para>
</change>

<change type="bugfix">
<para>
the Ruby module didn't respect the user locale for defaults in the Encoding
class.
</para>
</change>

<change type="bugfix">
<para>
the PHP 5 module failed to build with thread safety enabled; the bug had
appeared in 1.22.0.
</para>
</change>

</changes>


<changes apply="unit-python3.9" ver="1.22.0" rev="1"
         date="2021-02-04" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Python 3.9 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11 unit-jsc13
                unit-jsc14 unit-jsc15"
         ver="1.22.0" rev="1"
         date="2021-02-04" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.22.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.22.0" rev="1"
         date="2021-02-04" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
the ServerRequest and ServerResponse objects of Node.js module are now
compliant with Stream API.
</para>
</change>

<change type="feature">
<para>
support for specifying multiple directories in the "path" option of Python
apps.
</para>
</change>

<change type="bugfix">
<para>
a memory leak occurred in the router process when serving files larger than
128K; the bug had appeared in 1.13.0.
</para>
</change>

<change type="bugfix">
<para>
apps could stop processing new requests under high load; the bug had
appeared in 1.19.0.
</para>
</change>

<change type="bugfix">
<para>
app processes could terminate unexpectedly under high load; the bug had
appeared in 1.19.0.
</para>
</change>

<change type="bugfix">
<para>
invalid HTTP responses were generated for some unusual status codes.
</para>
</change>

<change type="bugfix">
<para>
the PHP_AUTH_USER, PHP_AUTH_PW, and PHP_AUTH_DIGEST server variables were
missing in the PHP module.
</para>
</change>

<change type="bugfix">
<para>
the router process could crash with multithreaded apps under high load.
</para>
</change>

<change type="bugfix">
<para>
Ruby apps with multithreading configured could crash on start under load.
</para>
</change>

<change type="bugfix">
<para>
mount points weren't unmounted when the "mount" namespace isolation was used;
the bug had appeared in 1.21.0.
</para>
</change>

<change type="bugfix">
<para>
the router process could crash while removing or reconfiguring an app that used
WebSocket.
</para>
</change>

<change type="bugfix">
<para>
a memory leak occurring in the router process when removing or reconfiguring
an application; the bug had appeared in 1.19.0.
</para>
</change>

</changes>


<changes apply="unit-jsc15" ver="1.21.0" rev="1"
         date="2020-11-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Java 15 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-jsc14" ver="1.21.0" rev="1"
         date="2020-11-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Java 14 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-jsc13" ver="1.21.0" rev="1"
         date="2020-11-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Java 13 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.21.0" rev="1"
         date="2020-11-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.21.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.21.0" rev="1"
         date="2020-11-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
procfs is mounted by default for all languages when "rootfs" isolation is used.
</para>
</change>

<change type="change">
<para>
any characters valid according to RFC 7230 are now allowed in HTTP header field
names.
</para>
</change>

<change type="change">
<para>
HTTP header fields with underscores ("_") are now discarded from requests by
default.
</para>
</change>

<change type="feature">
<para>
optional multithreaded request processing for Java, Python, Perl, and Ruby apps.
</para>
</change>

<change type="feature">
<para>
regular expressions in route matching patterns.
</para>
</change>

<change type="feature">
<para>
compatibility with Python 3.9.
</para>
</change>

<change type="feature">
<para>
the Python module now supports ASGI 2.0 legacy applications.
</para>
</change>

<change type="feature">
<para>
the "protocol" option in Python applications aids choice between ASGI and WSGI.
</para>
</change>

<change type="feature">
<para>
the fastcgi_finish_request() PHP function that finalizes request processing and
continues code execution without holding onto the client connection.
</para>
</change>

<change type="feature">
<para>
the "discard_unsafe_fields" HTTP option that enables discarding request header
fields with irregular (but still valid) characters in the field name.
</para>
</change>

<change type="feature">
<para>
the "procfs" and "tmpfs" automount isolation options to disable automatic
mounting of eponymous filesystems.
</para>
</change>

<change type="bugfix">
<para>
the router process could crash when running Go applications under high load;
the bug had appeared in 1.19.0.
</para>
</change>

<change type="bugfix">
<para>
some language dependencies could remain mounted after using "rootfs" isolation.
</para>
</change>

<change type="bugfix">
<para>
various compatibility issues in Java applications.
</para>
</change>

<change type="bugfix">
<para>
the Java module built with the musl C library couldn't run applications that
use "rootfs" isolation.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.20.0" rev="1"
         date="2020-10-08" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.20.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.20.0" rev="1"
         date="2020-10-08" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
the PHP module is now initialized before chrooting; this enables loading all
extensions from the host system.
</para>
</change>

<change type="change">
<para>
AVIF and APNG image formats added to the default MIME type list.
</para>
</change>

<change type="change">
<para>
functional tests migrated to the pytest framework.
</para>
</change>

<change type="feature">
<para>
the Python module now fully supports applications that use the ASGI 3.0 server
interface.
</para>
</change>

<change type="feature">
<para>
the Python module now has a built-in WebSocket server implementation for
applications, compatible with the HTTP &amp; WebSocket ASGI Message Format 2.1
specification.
</para>
</change>

<change type="feature">
<para>
automatic mounting of an isolated "/tmp" file system into chrooted application
environments.
</para>
</change>

<change type="feature">
<para>
the $host variable contains a normalized "Host" request value.
</para>
</change>

<change type="feature">
<para>
the "callable" option sets Python application callable names.
</para>
</change>

<change type="feature">
<para>
compatibility with PHP 8 RC 1. Thanks to Remi Collet.
</para>
</change>

<change type="feature">
<para>
the "automount" option in the "isolation" object allows to turn off the
automatic mounting of language module dependencies.
</para>
</change>

<change type="bugfix">
<para>
"pass"-ing requests to upstreams from a route was broken; the bug had appeared
in 1.19.0.  Thanks to 洪志道 (Hong Zhi Dao) for discovering and fixing it.
</para>
</change>

<change type="bugfix">
<para>
the router process could crash during reconfiguration.
</para>
</change>

<change type="bugfix">
<para>
a memory leak occurring in the router process; the bug had appeared in 1.18.0.
</para>
</change>

<change type="bugfix">
<para>
the "!" (non-empty) pattern was matched incorrectly;
the bug had appeared in 1.19.0.
</para>
</change>

<change type="bugfix">
<para>
fixed building on platforms without sendfile() support, notably NetBSD;
the bug had appeared in 1.16.0.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.19.0" rev="1"
         date="2020-08-13" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.19.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.19.0" rev="1"
         date="2020-08-13" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
reworked IPC between the router process and the applications to lower latencies,
increase performance, and improve scalability.
</para>
</change>

<change type="feature">
<para>
support for an arbitrary number of wildcards in route matching patterns.
</para>
</change>

<change type="feature">
<para>
chunked transfer encoding in proxy responses.
</para>
</change>

<change type="feature">
<para>
basic variables support in the "pass" option.
</para>
</change>

<change type="feature">
<para>
compatibility with PHP 8 Beta 1.
Thanks to Remi Collet.
</para>
</change>

<change type="bugfix">
<para>
the router process could crash while passing requests to an application under
high load.
</para>
</change>

<change type="bugfix">
<para>
a number of language modules failed to build on some systems;
the bug had appeared in 1.18.0.
</para>
</change>

<change type="bugfix">
<para>
time in error log messages from PHP applications could lag.
</para>
</change>

<change type="bugfix">
<para>
reconfiguration requests could hang if an application had failed to start;
the bug had appeared in 1.18.0.
</para>
</change>

<change type="bugfix">
<para>
memory leak during reconfiguration.
</para>
</change>

<change type="bugfix">
<para>
the daemon didn't start without language modules;
the bug had appeared in 1.18.0.
</para>
</change>

<change type="bugfix">
<para>
the router process could crash at exit.
</para>
</change>

<change type="bugfix">
<para>
Node.js applications could crash at exit.
</para>
</change>

<change type="bugfix">
<para>
the Ruby module could be linked against a wrong library version.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.18.0" rev="1"
         date="2020-05-28" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.18.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.18.0" rev="1"
         date="2020-05-28" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
the "rootfs" isolation option for changing root filesystem for an application.
</para>
</change>

<change type="feature">
<para>
multiple "targets" in PHP applications.
</para>
</change>

<change type="feature">
<para>
support for percent-encoding in the "uri" and "arguments" matching options
and in the "pass" option.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.17.0" rev="1"
         date="2020-04-16" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.17.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.17.0" rev="1"
         date="2020-04-16" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
a "return" action with optional "location" for immediate responses and external
redirection.
</para>
</change>

<change type="feature">
<para>
fractional weights support for upstream servers.
</para>
</change>

<change type="bugfix">
<para>
accidental 502 "Bad Gateway" errors might have occurred in applications under
high load.
</para>
</change>

<change type="bugfix">
<para>
memory leak in the router; the bug had appeared in 1.13.0.
</para>
</change>

<change type="bugfix">
<para>
segmentation fault might have occurred in the router process when reaching
open connections limit.
</para>
</change>

<change type="bugfix">
<para>
"close() failed (9: Bad file descriptor)" alerts might have appeared in the log
while processing large request bodies; the bug had appeared in 1.16.0.
</para>
</change>

<change type="bugfix">
<para>
existing application processes didn't reopen the log file.
</para>
</change>

<change type="bugfix">
<para>
incompatibility with some Node.js applications.
</para>
</change>

<change type="bugfix">
<para>
broken build on DragonFly BSD; the bug had appeared in 1.16.0.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.16.0" rev="1"
         date="2020-03-12" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.16.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.16.0" rev="1"
         date="2020-03-12" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
basic load-balancing support with round-robin.
</para>
</change>

<change type="feature">
<para>
a "fallback" option that performs an alternative action if a request can't be
served from the "share" directory.
</para>
</change>

<change type="feature">
<para>
reduced memory consumption by dumping large request bodies to disk.
</para>
</change>

<change type="feature">
<para>
stripping UTF-8 BOM and JavaScript-style comments from uploaded JSON.
</para>
</change>

<change type="bugfix">
<para>
negative address matching in router might work improperly in combination with
non-negative patterns.
</para>
</change>

<change type="bugfix">
<para>
Java Spring applications failed to run; the bug had appeared in 1.10.0.
</para>
</change>

<change type="bugfix">
<para>
PHP 7.4 was broken if it was built with thread safety enabled.
</para>
</change>

<change type="bugfix">
<para>
compatibility issues with some Python applications.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.15.0" rev="1"
         date="2020-02-06" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.15.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.15.0" rev="1"
         date="2020-02-06" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
extensions of dynamically requested PHP scripts were restricted to ".php".
</para>
</change>

<change type="feature">
<para>
compatibility with Ruby 2.7.
</para>
</change>

<change type="bugfix">
<para>
segmentation fault might have occurred in the router process with multiple
application processes under load; the bug had appeared in 1.14.0.
</para>
</change>

<change type="bugfix">
<para>
receiving request body over TLS connection might have stalled.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-python3.8
                unit-go
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.14.0" rev="1"
         date="2019-12-26" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.14.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.14.0" rev="1"
         date="2019-12-26" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
the Go package import name changed to "unit.nginx.org/go".
</para>
</change>

<change type="change">
<para>
Go package now links to libunit instead of including library sources.
</para>
</change>

<change type="feature">
<para>
ability to change user and group for isolated applications when Unit daemon
runs as an unprivileged user.
</para>
</change>

<change type="feature">
<para>
request routing by source and destination addresses and ports.
</para>
</change>

<change type="bugfix">
<para>
memory bloat on large responses.
</para>
</change>

</changes>


<changes apply="unit-go1.13" ver="1.13.0" rev="1"
         date="2019-11-20" time="09:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Go 1.13 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-go1.12" ver="1.13.0" rev="1"
         date="2019-11-20" time="09:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Go 1.12 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-python3.8" ver="1.13.0" rev="1"
         date="2019-11-18" time="14:20:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Python 3.8 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10 unit-go1.11
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.13.0" rev="1"
         date="2019-11-14" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.13.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.13.0" rev="1"
         date="2019-11-14" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
basic support for HTTP reverse proxying.
</para>
</change>

<change type="feature">
<para>
compatibility with Python 3.8.
</para>
</change>

<change type="bugfix">
<para>
memory leak in Python application processes when the close handler was used.
</para>
</change>

<change type="bugfix">
<para>
threads in Python applications might not work correctly.
</para>
</change>

<change type="bugfix">
<para>
Ruby on Rails applications might not work on Ruby 2.6.
</para>
</change>

<change type="bugfix">
<para>
backtraces for uncaught exceptions in Python 3 might be logged with significant
delays.
</para>
</change>

<change type="bugfix">
<para>
explicit setting a namespaces isolation option to false might have enabled it.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10 unit-go1.11
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.12.0" rev="1"
         date="2019-10-03" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.12.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.12.0" rev="1"
         date="2019-10-03" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
compatibility with PHP 7.4.
</para>
</change>

<change type="bugfix">
<para>
descriptors leak on process creation; the bug had appeared in 1.11.0.
</para>
</change>

<change type="bugfix">
<para>
TLS connection might be closed prematurely while sending response.
</para>
</change>

<change type="bugfix">
<para>
segmentation fault might have occurred if an irregular file was requested.
</para>
</change>

</changes>



<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10 unit-go1.11
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.11.0" rev="1"
         date="2019-09-19" time="18:00:00 +0300"
         packager="Konstantin Pavlov &lt;thresh@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.11.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.11.0" rev="1"
         date="2019-09-19" time="18:00:00 +0300"
         packager="Konstantin Pavlov &lt;thresh@nginx.com&gt;">

<change type="feature">
<para>
basic support for serving static files.
</para>
</change>

<change type="feature">
<para>
isolation of application processes with Linux namespaces.
</para>
</change>

<change type="feature">
<para>
built-in WebSocket server implementation for Java Servlet Containers.
</para>
</change>

<change type="feature">
<para>
direct addressing of API configuration options containing slashes "/"
using URI encoding (%2F).
</para>
</change>

<change type="bugfix">
<para>
segmentation fault might have occurred in Go applications under high load.
</para>
</change>

<change type="bugfix">
<para>
WebSocket support was broken if Unit was built with some linkers other than
GNU ld (e.g. gold or LLD).
</para>
</change>

</changes>


<changes apply="unit
                unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10 unit-go1.11
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.10.0" rev="2"
         date="2019-08-23" time="12:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="bugfix">
<para>
missed header files added to unit development package.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10 unit-go1.11
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.10.0" rev="1"
         date="2019-08-22" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.10.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.10.0" rev="1"
         date="2019-08-22" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
matching of cookies in routes made case sensitive.
</para>
</change>

<change type="change">
<para>
decreased log level of common errors when clients close connections.
</para>
</change>

<change type="change">
<para>
removed the Perl module's "--include=" ./configure option.
</para>
</change>

<change type="feature">
<para>
built-in WebSocket server implementation for Node.js module.
</para>
</change>

<change type="feature">
<para>
splitting PATH_INFO from request URI in PHP module.
</para>
</change>

<change type="feature">
<para>
request routing by scheme (HTTP or HTTPS).
</para>
</change>

<change type="feature">
<para>
support for multipart requests body in Java module.
</para>
</change>

<change type="feature">
<para>
improved API compatibility with Node.js 11.10 or later.
</para>
</change>

<change type="bugfix">
<para>
reconfiguration failed if "listeners" or "applications" objects were missing.
</para>
</change>

<change type="bugfix">
<para>
applying a large configuration might have failed.
</para>
</change>

</changes>


<changes apply="unit-go1.11" ver="1.9.0" rev="1"
         date="2019-05-30" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Go 1.11 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10
                unit-perl
                unit-ruby
                unit-jsc-common unit-jsc8 unit-jsc10 unit-jsc11"
         ver="1.9.0" rev="1"
         date="2019-05-30" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.9.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.9.0" rev="1"
         date="2019-05-30" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
request routing by arguments, headers, and cookies.
</para>
</change>

<change type="feature">
<para>
route matching patterns allow a wildcard in the middle.
</para>
</change>

<change type="feature">
<para>
POST operation for appending elements to arrays in configuration.
</para>
</change>

<change type="feature">
<para>
support for changing credentials using CAP_SETUID and CAP_SETGID capabilities
on Linux without running main process as privileged user.
</para>
</change>

<change type="bugfix">
<para>
memory leak in the router process might have happened when a client
prematurely closed the connection.
</para>
</change>

<change type="bugfix">
<para>
applying a large configuration might have failed.
</para>
</change>

<change type="bugfix">
<para>
PUT and DELETE operations on array elements in configuration did not work.
</para>
</change>

<change type="bugfix">
<para>
request schema in applications did not reflect TLS connections.
</para>
</change>

<change type="bugfix">
<para>
restored compatibility with Node.js applications that use
ServerResponse._implicitHeader() function; the bug had appeared in 1.7.
</para>
</change>

<change type="bugfix">
<para>
various compatibility issues with Node.js applications.
</para>
</change>

</changes>


<changes apply="unit-jsc-common" ver="1.8.0" rev="1"
         date="2019-03-01" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Java common packages for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-jsc8" ver="1.8.0" rev="1"
         date="2019-03-01" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Java 8 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-jsc10" ver="1.8.0" rev="1"
         date="2019-03-01" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Java 10 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-jsc11" ver="1.8.0" rev="1"
         date="2019-03-01" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Java 11 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10
                unit-perl
                unit-ruby"
         ver="1.8.0" rev="1"
         date="2019-03-01" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.8.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.8.0" rev="1"
         date="2019-03-01" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
now three numbers are always used for versioning: major, minor,
and patch versions.
</para>
</change>

<change type="change">
<para>
now QUERY_STRING is always defined even if the request does not include
the query component.
</para>
</change>

<change type="feature">
<para>
basic internal request routing by Host, URI, and method.
</para>
</change>

<change type="feature">
<para>
experimental support for Java Servlet Containers.
</para>
</change>

<change type="bugfix">
<para>
segmentation fault might have occurred in the router process.
</para>
</change>

<change type="bugfix">
<para>
various potential memory leaks.
</para>
</change>

<change type="bugfix">
<para>
TLS connections might have stalled.
</para>
</change>

<change type="bugfix">
<para>
some Perl applications might have failed to send the response body.
</para>
</change>

<change type="bugfix">
<para>
some compilers with specific flags might have produced non-functioning builds;
the bug had appeared in 1.5.
</para>
</change>

<change type="bugfix">
<para>
Node.js package had wrong version number when installed from sources.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6 unit-python3.7
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10
                unit-perl
                unit-ruby"
         ver="1.7.1" rev="1"
         date="2019-02-07" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.7.1.
</para>
</change>

</changes>


<changes apply="unit" ver="1.7.1" rev="1"
         date="2019-02-07" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="security">
<para>
a heap memory buffer overflow might have been caused in the router process by
a specially crafted request, potentially resulting in a segmentation fault or
other unspecified behavior (CVE-2019-7401).
</para>
</change>

<change type="bugfix">
<para>
install of Go module failed without prior building of Unit daemon;
the bug had appeared in 1.7.
</para>
</change>

</changes>


<changes apply="unit-python3.7" ver="1.7" rev="1"
         date="2018-12-25" time="10:30:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Python 3.7 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10
                unit-perl
                unit-ruby"
         ver="1.7" rev="1"
         date="2018-12-20" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.7.
</para>
</change>

</changes>


<changes apply="unit" ver="1.7" rev="1"
         date="2018-12-20" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
now rpath is set in Ruby module only if the library was not found in default
search paths; this allows to meet packaging restrictions on some systems.
</para>
</change>

<change type="bugfix">
<para>
"disable_functions" and "disable_classes" PHP options set via Control API
did not work.
</para>
</change>

<change type="bugfix">
<para>
Promises on request data in Node.js were not triggered.
</para>
</change>

<change type="bugfix">
<para>
various compatibility issues with Node.js applications.
</para>
</change>

<change type="bugfix">
<para>
a segmentation fault occurred in Node.js module if application tried to read
request body after request.end() was called.
</para>
</change>

<change type="bugfix">
<para>
a segmentation fault occurred in Node.js module if application attempted to
send header twice.
</para>
</change>

<change type="bugfix">
<para>
names of response header fields in Node.js module were erroneously treated as
case-sensitive.
</para>
</change>

<change type="bugfix">
<para>
uncatched exceptions in Node.js were not logged.
</para>
</change>

<change type="bugfix">
<para>
global install of Node.js module from sources was broken on some systems;
the bug had appeared in 1.6.
</para>
</change>

<change type="bugfix">
<para>
traceback for exceptions during initialization of Python applications might not
be logged.
</para>
</change>

<change type="bugfix">
<para>
PHP module build failed if PHP interpreter was built with thread safety
enabled.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10
                unit-perl
                unit-ruby"
         ver="1.6" rev="1"
         date="2018-11-15" time="18:00:00 +0300"
         packager="Konstantin Pavlov &lt;thresh@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.6.
</para>
</change>

</changes>


<changes apply="unit" ver="1.6" rev="1"
         date="2018-11-15" time="18:00:00 +0300"
         packager="Konstantin Pavlov &lt;thresh@nginx.com&gt;">

<change type="change">
<para>
"make install" now installs Node.js module as well if it was configured.
</para>
</change>

<change type="feature">
<para>
"--local" ./configure option to install Node.js module locally.
</para>
</change>

<change type="bugfix">
<para>
Node.js module might have crashed due to broken reference counting.
</para>
</change>

<change type="bugfix">
<para>
asynchronous operations in Node.js might not have worked.
</para>
</change>

<change type="bugfix">
<para>
various compatibility issues with Node.js applications.
</para>
</change>

<change type="bugfix">
<para>
"freed pointer is out of pool" alerts might have appeared in log.
</para>
</change>

<change type="bugfix">
<para>
module discovery did not work on 64-bit big-endian systems like IBM/S390x.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10
                unit-perl
                unit-ruby"
         ver="1.5" rev="1"
         date="2018-10-25" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.5.
</para>
</change>

</changes>


<changes apply="unit" ver="1.5" rev="1"
         date="2018-10-25" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
the "type" of application object for Go was changed to "external".
</para>
</change>

<change type="feature">
<para>
initial version of Node.js package with basic HTTP request-response support.
</para>
</change>

<change type="feature">
<para>
compatibility with LibreSSL.
</para>
</change>

<change type="feature">
<para>
--libdir and --incdir ./configure options to install libunit headers
and static library.
</para>
</change>

<change type="bugfix">
<para>
connection might be closed prematurely while sending response;
the bug had appeared in 1.3.
</para>
</change>

<change type="bugfix">
<para>
application processes might have stopped handling requests, producing
"last message send failed: Resource temporarily unavailable" alerts in log;
the bug had appeared in 1.4.
</para>
</change>

<change type="bugfix">
<para>
Go applications did not work when Unit was built with musl C library.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10
                unit-perl
                unit-ruby"
         ver="1.4" rev="2"
         date="2018-09-27" time="13:30:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="bugfix">
<para>
corrected instructions for launching sample application.
</para>
</change>

</changes>


<changes apply="unit" ver="1.4" rev="2"
         date="2018-09-27" time="13:30:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
the "saveconfig" and "loadconfig" action scripts were removed.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10
                unit-perl
                unit-ruby"
         ver="1.4" rev="1"
         date="2018-09-20" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.4.
</para>
</change>

</changes>


<changes apply="unit" ver="1.4" rev="1"
         date="2018-09-20" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
the control API maps the configuration object only at "/config/".
</para>
</change>

<change type="feature">
<para>
TLS support for client connections.
</para>
</change>

<change type="feature">
<para>
TLS certificates storage control API.
</para>
</change>

<change type="feature">
<para>
Unit library (libunit) to streamline language module integration.
</para>
</change>

<change type="feature">
<para>
"408 Request Timeout" responses while closing HTTP keep-alive connections.
</para>
</change>

<change type="feature">
<para>
improvements in OpenBSD support.
Thanks to David Carlier.
</para>
</change>

<change type="bugfix">
<para>
a segmentation fault might have occurred after reconfiguration.
</para>
</change>

<change type="bugfix">
<para>
building on systems with non-default locale might be broken.
</para>
</change>

<change type="bugfix">
<para>
"header_read_timeout" might not work properly.
</para>
</change>

<change type="bugfix">
<para>
header fields values with non-ASCII bytes might be handled incorrectly
in Python 3 module.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10
                unit-perl
                unit-ruby"
         ver="1.3" rev="1"
         date="2018-07-13" time="18:00:00 +0300"
         packager="Konstantin Pavlov &lt;thresh@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.3.
</para>
</change>

</changes>


<changes apply="unit" ver="1.3" rev="1"
         date="2018-07-13" time="18:00:00 +0300"
         packager="Konstantin Pavlov &lt;thresh@nginx.com&gt;">

<change type="change">
<para>
UTF-8 characters are now allowed in request header field values.
</para>
</change>

<change type="feature">
<para>
configuration of the request body size limit.
</para>
</change>

<change type="feature">
<para>
configuration of various HTTP connection timeouts.
</para>
</change>

<change type="feature">
<para>
Ruby module now automatically uses Bundler where possible.
</para>
</change>

<change type="feature">
<para>
http.Flusher interface in Go module.
</para>
</change>

<change type="bugfix">
<para>
various issues in HTTP connection errors handling.
</para>
</change>

<change type="bugfix">
<para>
requests with body data might be handled incorrectly in PHP module.
</para>
</change>

<change type="bugfix">
<para>
individual PHP configuration options specified via control API were reset
to previous values after the first request in application process.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9 unit-go1.10
                unit-perl
                unit-ruby"
         ver="1.2" rev="1"
         date="2018-06-07" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.2.
</para>
</change>

</changes>


<changes apply="unit" ver="1.2" rev="1"
         date="2018-06-07" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
configuration of environment variables for application processes.
</para>
</change>

<change type="feature">
<para>
customization of php.ini path.
</para>
</change>

<change type="feature">
<para>
setting of individual PHP configuration options.
</para>
</change>

<change type="feature">
<para>
configuration of execution arguments for Go applications.
</para>
</change>

<change type="bugfix">
<para>
keep-alive connections might hang after reconfiguration.
</para>
</change>

</changes>


<changes apply="unit-go1.10" ver="1.1" rev="1"
         date="2018-05-14" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Go 1.10 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9
                unit-perl
                unit-ruby"
         ver="1.1" rev="1"
         date="2018-04-26" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.1.
</para>
</change>

</changes>


<changes apply="unit" ver="1.1" rev="1"
         date="2018-04-26" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="bugfix">
<para>
Python applications that use the write() callable did not work.
</para>
</change>

<change type="bugfix">
<para>
virtual environments created with Python 3.3 or above might not have worked.
</para>
</change>

<change type="bugfix">
<para>
the request.Read() function in Go applications did not produce EOF
when the whole body was read.
</para>
</change>

<change type="bugfix">
<para>
a segmentation fault might have occurred while access log reopening.
</para>
</change>

<change type="bugfix">
<para>
in parsing of IPv6 control socket addresses.
</para>
</change>

<change type="bugfix">
<para>
loading of application modules was broken on OpenBSD.
</para>
</change>

<change type="bugfix">
<para>
a segmentation fault might have occurred when there were two modules
with the same type and version; the bug had appeared in 1.0.
</para>
</change>

<change type="bugfix">
<para>
alerts "freed pointer points to non-freeble page" might have appeared in log
on 32-bit platforms.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9
                unit-perl
                unit-ruby"
         ver="1.0" rev="1"
         date="2018-04-12" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 1.0.
</para>
</change>

</changes>


<changes apply="unit" ver="1.0" rev="1"
         date="2018-04-12" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
configuration object moved into "/config/" path.
</para>
</change>

<change type="feature">
<para>
basic access logging.
</para>
</change>

<change type="bugfix">
<para>
503 error occurred if Go application did not write response header or body.
</para>
</change>

<change type="bugfix">
<para>
Ruby applications that use encoding conversions might not have worked.
</para>
</change>

<change type="bugfix">
<para>
various stability issues.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9
                unit-perl"
         ver="0.7" rev="1"
         date="2018-03-22" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 0.7.
</para>
</change>

</changes>


<changes apply="unit-ruby" ver="0.7" rev="1"
         date="2018-04-02" time="15:40:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Ruby module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit" ver="0.7" rev="1"
         date="2018-03-22" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
Ruby application module.
</para>
</change>

<change type="bugfix">
<para>
in discovering modules.
</para>
</change>

<change type="bugfix">
<para>
various race conditions on reconfiguration and during shutting down.
</para>
</change>

<change type="bugfix">
<para>
tabs and trailing spaces were not allowed in header fields values.
</para>
</change>

<change type="bugfix">
<para>
a segmentation fault occurred in Python module if start_response() was called
outside of WSGI callable.
</para>
</change>

<change type="bugfix">
<para>
a segmentation fault might have occurred in PHP module if there was an error
while initialization.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9
                unit-perl"
         ver="0.6" rev="1"
         date="2018-02-09" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 0.6.
</para>
</change>

</changes>


<changes apply="unit" ver="0.6" rev="1"
         date="2018-02-09" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="bugfix">
<para>
the main process died when the "type" application option contained version;
the bug had appeared in 0.5.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9"
         ver="0.5" rev="1"
         date="2018-02-08" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 0.5.
</para>
</change>

</changes>


<changes apply="unit-perl" ver="0.5" rev="1"
         date="2018-02-08" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Perl module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit" ver="0.5" rev="1"
         date="2018-02-08" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
the "workers" application option was removed, the "processes"
application option should be used instead.
</para>
</change>

<change type="feature">
<para>
the "processes" application option with prefork and dynamic
process management support.
</para>
</change>

<change type="feature">
<para>
Perl application module.
</para>
</change>

<change type="bugfix">
<para>
in reading client request body; the bug had appeared in 0.3.
</para>
</change>

<change type="bugfix">
<para>
some Python applications might not have worked due to missing
"wsgi.errors" environ variable.
</para>
</change>

<change type="bugfix">
<para>
HTTP chunked responses might be encoded incorrectly on 32-bit
platforms.
</para>
</change>

<change type="bugfix">
<para>
infinite looping in HTTP parser.
</para>
</change>

<change type="bugfix">
<para>
segmentation fault in router.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7
                unit-python3.4 unit-python3.5 unit-python3.6
                unit-go unit-go1.7 unit-go1.8 unit-go1.9"
         ver="0.4" rev="1"
         date="2018-01-15" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 0.4.
</para>
</change>

</changes>


<changes apply="unit" ver="0.4" rev="1"
         date="2018-01-15" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
compatibility with DragonFly BSD.
</para>
</change>

<change type="feature">
<para>
"configure php --lib-static" option.
</para>
</change>

<change type="bugfix">
<para>
HTTP request body was not passed to application;
the bug had appeared in 0.3.
</para>
</change>

<change type="bugfix">
<para>
HTTP large header buffers allocation and deallocation fixed;
the bug had appeared in 0.3.
</para>
</change>

<change type="bugfix">
<para>
some PHP applications might not have worked with relative "root" path.
</para>
</change>

</changes>


<changes apply="unit-go1.9" ver="0.3" rev="1"
         date="2017-12-28" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Go 1.9 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-python3.6" ver="0.3" rev="1"
         date="2017-12-28" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Python 3.6 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php
                unit-python unit-python2.7 unit-python3.4 unit-python3.5
                unit-go unit-go1.7 unit-go1.8"
         ver="0.3" rev="1"
         date="2017-12-28" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
NGINX Unit updated to 0.3.
</para>
</change>

</changes>


<changes apply="unit" ver="0.3" rev="1"
         date="2017-12-28" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="change">
<para>
the Go package name changed to "nginx/unit".
</para>
</change>

<change type="change">
<para>
in the "limits.timeout" application option: application start time and
time in queue now are not accounted.
</para>
</change>

<change type="feature">
<para>
the "limits.requests" application option.
</para>
</change>

<change type="feature">
<para>
application request processing latency optimization.
</para>
</change>

<change type="feature">
<para>
HTTP keep-alive connections support.
</para>
</change>

<change type="feature">
<para>
the "home" Python virtual environment configuration option.
</para>
</change>

<change type="feature">
<para>
Python atexit hook support.
</para>
</change>

<change type="feature">
<para>
various Go package improvements.
</para>
</change>

<change type="bugfix">
<para>
various crashes fixed.
</para>
</change>

</changes>


<changes apply="unit-go" ver="0.2" rev="1"
         date="2017-10-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Go module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-go1.7" ver="0.2" rev="1"
         date="2017-10-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Go 1.7 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-go1.8" ver="0.2" rev="1"
         date="2017-10-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Go 1.8 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-python" ver="0.2" rev="1"
         date="2017-10-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Python module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-python2.7" ver="0.2" rev="1"
         date="2017-10-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Python 2.7 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-python3.4" ver="0.2" rev="1"
         date="2017-10-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Python 3.4 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-python3.5" ver="0.2" rev="1"
         date="2017-10-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of Python 3.5 module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit-php" ver="0.2" rev="1"
         date="2017-10-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
Initial release of PHP module for NGINX Unit.
</para>
</change>

</changes>


<changes apply="unit" ver="0.2" rev="1"
         date="2017-10-19" time="18:00:00 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change type="feature">
<para>
configuration persistence.
</para>
</change>

<change type="feature">
<para>
improved handling of configuration errors.
</para>
</change>

<change type="feature">
<para>
application "timeout" property.
</para>
</change>

<change type="bugfix">
<para>
POST request for PHP were handled incorrectly.
</para>
</change>

<change type="bugfix">
<para>
the router exited abnormally if all listeners had been deleted.
</para>
</change>

<change type="bugfix">
<para>
the router crashed under load.
</para>
</change>

<change type="bugfix">
<para>
memory leak in the router.
</para>
</change>

</changes>


<changes apply="unit" ver="0.1" rev="1"
         date="2017-09-06" time="19:14:28 +0300"
         packager="Andrei Belov &lt;defan@nginx.com&gt;">

<change>
<para>
First public release.
</para>
</change>

</changes>


</change_log>
